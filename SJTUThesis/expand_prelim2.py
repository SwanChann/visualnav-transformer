#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""Add more content to preliminaries.tex to reach ~380 lines."""
import os

outpath = os.path.join(os.path.dirname(os.path.abspath(__file__)), 'contents', 'preliminaries.tex')
with open(outpath, 'r', encoding='utf-8') as f:
    c = f.read()

print(f'Before: {c.count(chr(10))+1} lines')

replacements = [
    # 1. After closed-loop discussion
    (
        '这对于行走不稳定的足式平台尤为重要。',
        '这对于行走不稳定的足式平台尤为重要。\n\n'
        '形式化地，闭环执行可以描述为一个循环过程。在第 $n$ 个控制周期：'
        '（1）采集当前观察 $\\mathcal{O}_{t_n}$；'
        '（2）运行策略 $\\pi$ 得到轨迹 $\\mathbf{A}_{t_n} = [\\mathbf{a}_1, \\ldots, \\mathbf{a}_H]$；'
        '（3）仅将前 $H_{\\text{exec}}$ 个 waypoint 转换为速度指令并发送给底层控制器；'
        '（4）等待下一个观察周期到来，重复上述过程。'
        '这种"规划长、执行短"的策略在模型预测控制（Model Predictive Control, MPC）领域'
        '有着悠久的传统，扩散策略在此框架下的应用可以视为将传统 MPC 中的优化求解替换为'
        '条件扩散采样。'
    ),
    # 2. Before "这种建模方式的优势在于"
    (
        '从而提升了生成质量。\n\n这种建模方式的优势在于',
        '从而提升了生成质量。\n\n'
        '在轨迹生成的具体应用中，$x_0$ 不再是图像像素，而是轨迹 waypoint 序列 '
        '$\\mathbf{A} = [\\mathbf{a}_1, \\ldots, \\mathbf{a}_H] \\in \\mathbb{R}^{H \\times 2}$。'
        '相应地，扩散过程在轨迹空间中进行：前向过程逐步向轨迹添加噪声，反向过程从纯噪声'
        '逐步恢复出合理轨迹。由于轨迹空间的维度（$H \\times 2$，在 NoMaD 中通常为 '
        '$8 \\times 2 = 16$ 维）远低于图像空间（如 $85 \\times 85 \\times 3 = 21675$ 维），'
        '扩散模型在轨迹生成中的收敛速度和采样效率都显著优于图像生成，这也是扩散策略能够'
        '在实时控制场景中应用的重要原因。\n\n这种建模方式的优势在于'
    ),
    # 3. Before DDPM-DDIM comparison table
    (
        '提供了在采样多样性与确定性之间的连续调节能力。\n\n\\begin{table}[htbp]\n    \\centering\n    \\caption{DDPM 与 DDIM',
        '提供了在采样多样性与确定性之间的连续调节能力。\n\n'
        '在导航轨迹生成的实际应用中，$\\eta$ 的选择涉及探索与确定性之间的权衡。'
        '$\\eta = 0$（完全确定性）意味着给定相同的初始噪声和条件，每次都会生成完全相同的'
        '轨迹，这在需要可复现行为的场景中有优势，但丧失了多样性。$\\eta > 0$ 则引入随机性，'
        '使得每次采样可能产生不同的轨迹候选，这对于 TTS 中需要多条候选的场景是必要的。'
        '在本文实验中，当使用 TTS 时通常保留适度的随机性（$\\eta \\in [0.3, 0.5]$），'
        '以确保候选轨迹之间有足够的多样性。\n\n\\begin{table}[htbp]\n    \\centering\n    \\caption{DDPM 与 DDIM'
    ),
    # 4. After DDIM acceleration discussion
    (
        '而是"更快之后轨迹分布是否仍可接受"。',
        '而是"更快之后轨迹分布是否仍可接受"。\n\n'
        '在实际使用中，DDIM 的跳步采样通过选择原始时间步序列 $\\{1, 2, \\ldots, T\\}$ '
        '的一个子序列 $\\{\\tau_1, \\tau_2, \\ldots, \\tau_S\\}$（其中 $S \\ll T$）来实现加速。'
        '例如当 $T=100$ 而选择 $S=10$ 时，每次去噪跨越约 10 个原始时间步。'
        '子序列的选择方式（均匀间隔、二次间隔等）也会影响采样质量。'
        '在本文的导航场景中，由于轨迹空间的维度远低于图像空间，扩散模型对跳步采样的'
        '容忍度通常较高，这为在边缘设备上实现实时推理提供了可能。'
    ),
    # 5. After MCTS comparison
    (
        '其计算开销的扩展方式更为简单和可控。',
        '其计算开销的扩展方式更为简单和可控。\n\n'
        '从理论角度来看，TTS 的有效性建立在一个关键假设之上：扩散模型生成的候选集合中，'
        '高质量轨迹的存在概率随候选数量 $N$ 的增加而增大。设单次采样得到可接受轨迹的'
        '概率为 $p$，则 $N$ 次独立采样后至少存在一条可接受轨迹的概率为 $1 - (1-p)^N$。'
        '当 $p$ 较小时（对应困难场景），需要较大的 $N$ 才能保证成功率；当 $p$ 接近 1 时'
        '（对应简单场景），$N=1$ 即可，此时 TTS 退化为普通单次采样。这种"按需扩展"的'
        '特性使得 TTS 特别适合于计算预算可变的部署场景。'
    ),
    # 6. After goal mask exploration discussion
    (
        '这种设计使得探索能力与导航增强能力共享同一训练框架。',
        '这种设计使得探索能力与导航增强能力共享同一训练框架。\n\n'
        '在部署阶段，探索模式与导航模式的切换仅需改变推理时的条件输入：提供真实目标 '
        'token 时进入导航模式，提供掩码 token 时进入探索模式。两种模式之间的切换不需要'
        '加载不同的模型权重或改变网络结构，这大大简化了部署系统的设计。此外，探索模式下'
        '生成的轨迹并非随机——它们反映了训练数据中的轨迹分布特征，通常表现为沿走廊前进、'
        '绕过障碍物等符合环境结构的行为模式。这意味着探索模式可以作为"安全回退"策略：'
        '当目标不确定或距离预测失效时，系统可以切换到探索模式，生成合理的前进行为而非停止不动。'
    ),
    # 7. After UDP MTU discussion
    (
        '因此不存在分包问题。',
        '因此不存在分包问题。在实现层面，本文使用 Python 的 socket 库创建 UDP 套接字，'
        '每个控制周期通过 \\texttt{sendto} 方法发送包含三维速度指令（前后速度、左右速度、'
        '偏航角速度）的二进制数据包。接收端（Lite3 主控板）以固定频率轮询 UDP 端口，'
        '解析速度指令后转发给底层步态控制器。为保证通信可靠性，系统在应用层实现了简单的'
        '心跳机制：导航主机每个控制周期都会发送指令（即使速度为零），接收端若超过 '
        '$250\\,\\text{ms}$ 未收到新指令则自动执行零速停止。'
    ),
    # 8. After frequency hierarchy discussion
    (
        '触发超时保护机制。\n\n这些参数并非纯说明信息',
        '触发超时保护机制。\n\n'
        '这种多层频率架构可以用以下层级关系概括：\n'
        '\\begin{equation}\n'
        'f_{\\text{joint}} \\gg f_{\\text{velocity}} \\gg f_{\\text{vision}},\n'
        '\\end{equation}\n'
        '其中 $f_{\\text{joint}} \\approx 50\\text{--}200\\,\\text{Hz}$ 为关节级控制频率，'
        '$f_{\\text{velocity}} \\approx 50\\,\\text{Hz}$ 为速度指令接收频率，'
        '$f_{\\text{vision}} = 4\\,\\text{Hz}$ 为视觉导航策略的运行频率。'
        '这种频率分离设计的好处是每一层只需关注自身时间尺度上的决策，坏处是层间的响应延迟'
        '会累积。从高层视觉策略发出速度指令到机器人实际开始执行新动作，中间存在至少一个'
        '底层控制周期（$5\\text{--}20\\,\\text{ms}$）的延迟，但在 $4\\,\\text{Hz}$ 的高层'
        '周期（$250\\,\\text{ms}$）尺度上，这一延迟可以忽略。\n\n'
        '这些参数并非纯说明信息'
    ),
    # 9. After legged robot visual jitter
    (
        '从而减轻视觉抖动的影响。',
        '从而减轻视觉抖动的影响。此外，足式平台的非完整运动学约束也与轮式平台不同。'
        '轮式机器人通常可以精确执行差速运动学模型所描述的运动，而四足机器人的实际运动'
        '轨迹受步态规划器和地形交互的影响，与指令速度之间存在非线性偏差。这种偏差意味着'
        '即使高层策略输出了精确的速度指令，机器人的实际位移也可能与预期存在差异，'
        '进一步强调了闭环执行和频繁重规划的必要性。'
    ),
    # 10. After encoder coupling end-to-end finetuning
    (
        '同时更新编码器适配层、距离预测头和扩散策略头的参数。',
        '同时更新编码器适配层、距离预测头和扩散策略头的参数。这种端到端微调策略虽然'
        '计算成本较高，但确保了三个组件在新的特征空间中重新对齐，避免了因局部更新导致'
        '的性能退化。在第三章的实验中，本文将详细对比不同微调策略（仅微调适配层 vs. '
        '端到端微调）对导航性能的影响。'
    ),
]

for old, new in replacements:
    if old in c:
        c = c.replace(old, new, 1)
        print(f'OK: {old[:50]}...')
    else:
        print(f'MISS: {old[:50]}...')

with open(outpath, 'w', encoding='utf-8') as f:
    f.write(c)

print(f'After: {c.count(chr(10))+1} lines')
