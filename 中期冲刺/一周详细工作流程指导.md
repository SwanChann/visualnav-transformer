# NoMaD 中期冲刺：一周详细工作流程指导

> **训练环境**：Ubuntu 22.04 · 单卡 RTX 4090 (24 GB) · CUDA 12.x  
> **仿真环境**：Ubuntu 20.04 · PyBullet / Isaac Gym  
> **基线项目**：visualnav-transformer（NoMaD）  
> **起始日期**：2026-03-24

### ⚠️ 远程部署路径说明

> 本文档中所有代码均在 **远程 Ubuntu 训练机** 上执行（通过 SSH 连接），而非本地 Windows。
>
> **关键路径约定**：
>
> | 角色 | 绝对路径 |
> |------|---------|
> | 项目根目录（训练机） | `/home/yifei/codespace/visualnav-transformer` |
> | 脚本目录 | `/home/yifei/codespace/visualnav-transformer/scripts/` |
> | 实验结果输出目录 | `/home/yifei/codespace/visualnav-transformer/results/` |
> | 预训练权重 | `/home/yifei/codespace/visualnav-transformer/deployment/model_weights/nomad/nomad.pth`（自训练后复制到此处） |
> | 数据集 | `/home/yifei/codespace/visualnav-transformer/nomad_dataset/go_stanford/` |
> | 仿真资产目录（仿真机） | `/home/yifei/codespace/visualnav-transformer/assets/lite3/` |
>
> **脚本放置规则**：
> 将本文中所有 `scripts/` 下的 `.py` 文件上传到训练机的  
> `/home/yifei/codespace/visualnav-transformer/scripts/` 目录。
>
> **统一运行方式**：
> ```bash
> # 先 SSH 连接训练机，激活环境
> ssh yifei@<训练机IP>
> conda activate nomad
>
> # 所有脚本均在项目根目录下运行
> cd /home/yifei/codespace/visualnav-transformer
> python scripts/<脚本名>.py
> ```

---

## 目录

- [📊 工作内容可视化——实验指标总览](#-工作内容可视化实验指标总览)
- [核心研究问题与科研空白](#核心研究问题与科研空白)
- [Day 1 环境搭建 + 基线跑通 + 文献空白梳理](#day-1周一环境搭建--基线跑通--文献空白梳理)
- [Day 2 DDIM 加速采样——满足足式控制频率需求](#day-2周二ddim-加速采样满足足式控制频率需求)
- [Day 3 CFG 目标引导 + 步态抖动鲁棒性实验](#day-3周三cfg-目标引导--步态抖动鲁棒性实验)
- [Day 4 视觉编码器升级（DINOv2）——增强跨形态泛化](#day-4周四视觉编码器升级dinov2增强跨形态泛化)
- [Day 5 足式机器人仿真适配 + 轮式 vs 足式对比（Ubuntu 20.04）](#day-5周五足式机器人仿真适配--轮式-vs-足式对比ubuntu-2004)
- [Day 6 面向足式部署的整合消融研究](#day-6周六面向足式部署的整合消融研究)
- [Day 7 论文叙事构建 + 研究贡献总结](#day-7周日论文叙事构建--研究贡献总结)
- [附录 文件夹最终结构](#附录中期冲刺文件夹最终结构)

---

## 📊 工作内容可视化——实验指标总览

> 本节汇总一周所有实验产生的**定量指标**，分为两大部分：  
> **Part A** 代码运行计算得到的各项指标（含指标含义解读）；  
> **Part B** 仿真运行结果（Day 5 轮式 vs 足式对比）。  
> 每张表格对应一个可运行脚本，数据来源标注在表头。

---

### Part A：代码实验指标汇总

#### A1. Day 1 — 数据集完整性检查（`scripts/check_dataset.py`）

**运行命令**：`python scripts/check_dataset.py`

| 指标 | 含义 | 预期值 |
| ---- | ---- | ------ |
| `traj_count` | 数据集中轨迹总数 | ≈ 110+ 条 |
| `ok_count` | 通过完整性检查的轨迹数 | = `traj_count`（全部通过） |
| `bad_count` | 缺失 `traj_data.pkl` 的轨迹数 | 0 |
| `n_pos` / `n_yaw` | 每条轨迹的位置/偏航序列长度 | 50–200（随轨迹长度变化） |
| `n_images` | 每条轨迹的 jpg 图片数量 | 应与 `n_pos` 基本一致 |

**输出示例表格**（`results/day1/.../check_results.txt`）：

| traj_name | status | n_pos | n_yaw | n_images |
| --------- | ------ | ----- | ----- | -------- |
| no10vc_10_0 | OK | 68 | 68 | 68 |
| no10vc_11_0 | OK | 124 | 124 | 124 |
| ... | ... | ... | ... | ... |
| **汇总** | — | — | — | — |

> **解读**：全部 `OK` 说明数据完整可用；若有 `MISSING`，需重新下载对应轨迹。

---

#### A2. Day 1 — 离线推理基线（`scripts/offline_inference.py`）

**运行命令**：`python scripts/offline_inference.py`

| 指标 | 含义 | 预期值 | 单位 |
| ---- | ---- | ------ | ---- |
| `model_params` | NoMaD 模型总参数量 | ≈ 50 M | 个 |
| `ddpm_inference_time` | DDPM-10 步单次推理耗时 | 50–100 | ms |
| `inference_frequency` | 推理频率 $\frac{1}{\text{inference\_time}}$ | 10–20 | Hz |
| `num_samples` | 并行采样的轨迹数 | 8 | 条 |
| `len_traj_pred` | 每条轨迹的路径点数 | 8 | 个 |
| `predicted_distance` | 模型预测到目标的距离 | 0.1–2.0 | 归一化单位 |

**输出示例**（`results/day1/.../summary.txt`）：

| 项目 | 值 |
| ---- | -- |
| DDPM inference time | 72 ms (10 steps) |
| Inference frequency | 13.9 Hz |
| Generated trajectories | 8 × 8 waypoints |
| Predicted distance | 0.4523 |

> **解读**：
> - **推理频率 10–20 Hz** 与轮式平台 4–10 Hz 需求匹配，但对足式高层导航 (≥15 Hz) 略显不足 → Day 2 DDIM 加速动机。
> - **predicted_distance** 越小表示模型认为离目标越近，数值范围取决于训练数据归一化方式。

**可视化产出**：`baseline_trajectories.png` + `explore_trajectories.png`（导航 vs 探索模式轨迹对比图）

---

#### A3. Day 2 — DDIM 加速采样实验（`scripts/ddim_experiment.py`）

**运行命令**：`python scripts/ddim_experiment.py`

**核心对比表**（`results/day2/results.txt`）：

| Config | Time (ms) ↓ | Std (ms) | MSE vs Baseline ↓ | Diversity | Steps | Speedup ↑ |
| ------ | ----------- | -------- | ------------------ | --------- | ----- | --------- |
| DDPM-10 (baseline) | ~72 | ~5 | 0.000000 | ~0.08 | 10 | 1.00× |
| DDIM-10 | ~70 | ~4 | ~0.0001 | ~0.07 | 10 | ~1.03× |
| DDIM-5 | ~38 | ~3 | ~0.0005 | ~0.06 | 5 | ~1.9× |
| DDIM-3 | ~25 | ~2 | ~0.002 | ~0.05 | 3 | ~2.9× |
| DDIM-2 | ~18 | ~2 | ~0.01 | ~0.04 | 2 | ~4.0× |
| DDIM-1 | ~12 | ~1 | ~0.05 | ~0.02 | 1 | ~6.0× |

> **指标含义**：
>
> | 指标 | 含义 | 如何解读 |
> | ---- | ---- | -------- |
> | `Time (ms)` | 单次推理平均延迟 | 越低 → 决策越快，对足式实时性越有利 |
> | `MSE vs Baseline` | 与 DDPM-10 基线轨迹的均方误差 | 越低 → 加速后质量损失越小 |
> | `Diversity` | 采样轨迹端点标准差 | 越高 → 策略探索能力越强；过低则退化为确定性 |
> | `Speedup` | 相对基线加速比 $\frac{T_{\text{baseline}}}{T_{\text{config}}}$ | >2× 为足式有价值 |
>
> **关键判断阈值**（对应足式部署需求）：
>
> | 控制场景 | 最低频率需求 | 对应最大延迟 |
> | -------- | ------------ | ------------ |
> | 轮式底盘导航 | 4–10 Hz | 100–250 ms |
> | 四足高层导航 | **≥15 Hz** | **≤67 ms** |
> | 四足关节控制 | 100–1000 Hz | 由步态控制器处理 |

**可视化产出**：`ddim_comparison.png`（三面板：推理时间柱状图 / MSE 柱状图 / 轨迹对比图）

---

#### A4. Day 2 — 实时摄像头推理（`scripts/realtime_inference.py`）

**运行命令**：`python scripts/realtime_inference.py [--ddim-steps 5]`

**统计表**（`results/realtime/.../summary.txt`）：

| 指标 | 含义 | 预期值 |
| ---- | ---- | ------ |
| `Avg Inference FPS` | 平均推理帧率（含采集+编码+扩散全流程） | 15–25 Hz（DDIM-5） |
| `Min FPS` | 最低帧率（最差情况） | ≥10 Hz |
| `Max FPS` | 最高帧率 | ≤30 Hz |
| `Total Frames` | 总处理帧数 | 视运行时长 |
| `DDIM Steps` | 使用的去噪步数 | 5（推荐） |

> **解读**：
> - **Avg FPS ≥ 15 Hz** → ✅ 满足四足高层导航实时性要求
> - **Avg FPS < 15 Hz** → ⚠️ 需进一步降低 DDIM 步数或减少采样数

**可视化产出**：`fps_trend.png`（帧率趋势图，含移动平均线和 15 Hz / 10 Hz 阈值线）

---

#### A5. Day 3 — CFG 目标引导实验（`scripts/cfg_experiment.py`）

**运行命令**：`python scripts/cfg_experiment.py`

**核心对比表**（`results/day3/results.txt`）：

| w (引导强度) | Diversity ↓ | Mean Displacement ↑ | Time (ms) |
| ------------ | ----------- | ------------------- | --------- |
| −1.0（纯探索） | ~0.12 | ~0.08 | ~130 |
| 0.0（标准条件） | ~0.08 | ~0.15 | ~72 |
| 0.5 | ~0.06 | ~0.18 | ~130 |
| 1.0 | ~0.04 | ~0.22 | ~130 |
| 2.0 | ~0.03 | ~0.28 | ~130 |
| 4.0 | ~0.01 | ~0.32 | ~130 |

> **指标含义**：
>
> | 指标 | 含义 | 如何解读 |
> | ---- | ---- | -------- |
> | `w` | CFG 引导强度 $\epsilon_\text{final}=(1+w)\epsilon_\text{cond}-w\epsilon_\text{uncond}$ | `w=−1` 纯探索基线；`w=0` 标准条件基线 |
> | `Diversity` | 轨迹端点标准差 | w↑ → Diversity↓（轨迹更集中于目标方向） |
> | `Mean Displacement` | 平均轨迹位移量（离原点距离） | w↑ → 位移↑（更积极朝目标移动） |
> | `Time (ms)` | 推理延迟；`w≠0` 时每步需**两次**前向传播 | 约为 `w=0` 的 1.8× |
>
> **实验基线**：`w = −1`（纯探索）与 `w = 0`（标准条件）分别提供下界和上界参照。**最佳区间**：`w = 1.0 ~ 2.0`。

**可视化产出**：
- `cfg_comparison.png` — 6 个 w 值的轨迹分布对比（2×3 网格）
- `cfg_trends.png` — Diversity / Mean Displacement / Time 三趋势图
- `cfg_w{-1.0~4.0}.png` — 每个 w 值独立轨迹图

---

#### A6. Day 3 — 步态抖动鲁棒性实验（`scripts/gait_shake_robustness.py`）

**运行命令**：`python scripts/gait_shake_robustness.py`

**核心对比表**（`results/day3/.../results.txt`）：

| 抖动等级 | 参数（角度/偏移） | cos_sim ↑ | MSE ↓ | Heading Dev. ↓ |
| -------- | ----------------- | --------- | ----- | -------------- |
| no_shake（基线） | 0° / 0px | 1.0000（自身） | 0.000000 | 0.00° |
| mild_shake（行走） | ±1.5° / ±2,3px | ~0.97 | ~0.001 | ~2° |
| moderate_shake（快走） | ±3.0° / ±4,5px | ~0.93 | ~0.005 | ~5° |
| severe_shake（小跑） | ±5.0° / ±6,8px | ~0.87 | ~0.02 | ~10° |

> **指标含义**：
>
> | 指标 | 含义 | 足式部署评判标准 |
> | ---- | ---- | ---------------- |
> | `cos_sim` | 抖动轨迹与 no_shake 基线轨迹的余弦相似度 | > 0.95 ✅ 鲁棒 / < 0.90 ❌ 需补偿 |
> | `MSE` | 与 no_shake 基线的轨迹均方误差 | 越低越好 |
> | `Heading Dev.` | 第一个路径点的航向偏差 (度) | < 5° 可接受 / > 10° 危险 |
>
> **基线说明**：no_shake 与各抖动条件走**完全相同**的处理流水线（PIL 加载 → 抖动 → 归一化），
> 只是抖动参数为零。不存在独立的 baseline 计算路径——no_shake 的 cos_sim 恒为 1.0（自身对比）。
>
> **足式部署判断**：
> - **行走步态 (2.5 Hz)**：`cos_sim > 0.95` → 策略天然鲁棒，无需额外处理
> - **小跑步态 (3.5 Hz)**：`cos_sim < 0.90` → 需要 CFG 补偿或图像稳定
>
> **进阶验证**：在 `moderate_shake` 下追加 CFG（`w=1.0`）推理，
> 若 `cos_sim` 从 ~0.93 回升至 >0.95，则 **CFG 对足式部署具有功能性价值**。

**可视化产出**：`gait_shake_robustness.png`（轨迹对比 + 余弦相似度柱状图）

---

#### A7. Day 4 — DINOv2 训练验证（`scripts/train_dinov2.py`）

**运行命令**：`python ../scripts/train_dinov2.py -c config/nomad_dinov2.yaml`（在 `train/` 目录下执行）

**训练监控指标**（wandb 面板 / 终端输出）：

| 指标 | 含义 | 预期趋势 |
| ---- | ---- | -------- |
| `train_loss` | 训练集总损失（扩散损失 + 距离损失） | 前 5 epoch 显著下降 |
| `test_loss` | 测试集总损失 | 与 train_loss 趋势一致，不发散 |
| `total_params` | 模型总参数量 | ~50 M（DINOv2-S 21M + NoMaD 头部） |
| `trainable_params` | 可训练参数数量 | ~29 M（冻结 DINOv2 时）/ ~50 M（解冻时） |
| `trainable_ratio` | 可训练比例 | 冻结时 ~58% / 解冻时 ~100% |

**DINOv2 vs EfficientNet 对比表**（训练完成后手动整理）：

| 编码器 | 参数量 | 最终 test_loss | 收敛 epoch | 训练时间 | 输入尺寸 |
| ------ | ------ | -------------- | ---------- | -------- | -------- |
| EfficientNet-B0（原版） | 5.3 M | baseline | ~100 | ~8 h | 96×96 |
| DINOv2-Small（冻结） | 21 M | 待填写 | 待填写 | ~3 h (20 ep) | 98×98 |
| DINOv2-Small（解冻末 2 层） | 21 M | 待填写 | 待填写 | ~4 h (20 ep) | 98×98 |

> **解读**：
> - **test_loss 低于 EfficientNet** → DINOv2 自监督特征对导航任务有优势
> - **test_loss 更高或不收敛** → 冻结过多，需解冻最后 2 层重跑
> - **跨形态价值**：DINOv2 的自监督特征更关注结构信息（轮廓、空间关系），理论上对不同视高、视野的泛化能力更好

---

#### A8. Day 6 — 综合消融实验（`scripts/ablation_experiment.py`）

**运行命令**：`python scripts/ablation_experiment.py [--num-runs 5]`

**完整消融矩阵**（3 采样器 × 3 引导强度 × 4 抖动等级 = **36 组配置**）：

**核心结果摘要表**（`results/day6/ablation_summary.txt`）：

| 配置 | 采样器 | w | 抖动 | Time (ms) | MSE ↓ | cos_sim ↑ | Heading Dev. ↓ | Diversity |
| ---- | ------ | - | ---- | --------- | ----- | --------- | -------------- | --------- |
| C1 基线 | DDPM-10 | 0 | 无 | ~72 | 0 | 1.00 | 0° | ~0.08 |
| C5 推荐 | DDIM-5 | 1.0 | 无 | ~70 | ~0.001 | ~0.98 | ~1° | ~0.04 |
| C5+抖动 | DDIM-5 | 1.0 | 中等 | ~70 | ~0.008 | ~0.94 | ~4° | ~0.04 |
| C5+严重 | DDIM-5 | 1.0 | 严重 | ~70 | ~0.02 | ~0.89 | ~8° | ~0.04 |
| C3 极限 | DDIM-3 | 2.0 | 无 | ~48 | ~0.005 | ~0.96 | ~2° | ~0.02 |
| ... | ... | ... | ... | ... | ... | ... | ... | ... |

> **消融指标含义总结**：
>
> | 指标 | 英文 | 物理含义 | 足式部署意义 |
> | ---- | ---- | -------- | ------------ |
> | `inference_ms` | Inference Time | 单次推理延迟 | 决定决策频率，≤67 ms 满足 ≥15 Hz |
> | `diversity` | Trajectory Diversity | 采样轨迹端点方差 | 过低则策略退化为确定性，失去扩散优势 |
> | `mse_vs_clean` | MSE vs Clean Baseline | 与无抖动基线轨迹的均方误差 | 衡量抖动鲁棒性，越低越好 |
> | `cos_sim` | Cosine Similarity | 抖动/CFG 轨迹与基线方向一致性 | >0.95 鲁棒 / <0.90 需补偿 |
> | `heading_dev` | Heading Deviation | 第一个路径点的方向偏差 | <5° 安全 / >10° 危险 |
> | `mean_disp` | Mean Displacement | 轨迹平均位移（目标导向性） | 越大说明越积极朝目标移动 |

**可视化产出**：
- `ablation_heatmap.png` — 采样器 × 引导强度热力图（颜色 = MSE 或 cos_sim）
- `ablation_shake_robustness.png` — 抖动等级对各配置的影响曲线
- `ablation_pareto.png` — 速度 vs 质量 Pareto 前沿图
- `ablation_radar.png` — Top-3 最佳配置的多维雷达图

---

### Part B：仿真运行结果

#### B1. Day 5 — 开环仿真轨迹（`scripts/lite3_sim.py`）

**运行命令**：`python scripts/lite3_sim.py`

**路径点执行日志**（`results/day5/.../lite3_open_loop.txt`）：

| step | wp_dx | wp_dy | cmd_v (m/s) | cmd_w (rad/s) | pos_x (m) | pos_y (m) |
| ---- | ----- | ----- | ----------- | ------------- | --------- | --------- |
| 0 | 0.050 | 0.010 | 0.200 | 0.792 | — | — |
| 1 | 0.100 | 0.020 | 0.400 | 0.792 | — | — |
| ... | ... | ... | ... | ... | ... | ... |
| 7 | 0.400 | 0.000 | 1.600 | 0.000 | — | — |

> **指标含义**：
>
> | 列名 | 含义 |
> | ---- | ---- |
> | `wp_dx` / `wp_dy` | NoMaD 预测的路径点位移 (m)，前进/侧向 |
> | `cmd_v` | PD 控制器输出的线速度指令 (m/s) |
> | `cmd_w` | PD 控制器输出的角速度指令 (rad/s) |
> | `pos_x` / `pos_y` | 执行后机器人在世界坐标系中的位置 (m) |
> | `Total Displacement` | 起点到终点的欧氏距离 (m) |

---

#### B2. Day 5 — 轮式 vs 足式平台对比（`scripts/lite3_sim.py --compare`）

**运行命令**：`python scripts/lite3_sim.py --compare`

**平台对比表**（`results/day5/.../wheeled_vs_legged.txt`）：

| Platform | Path Length (m) ↑ | Final Disp. (m) | Lat. Dev. (m) ↓ |
| -------- | ----------------- | --------------- | ---------------- |
| Wheeled (Husky) | 待填写 | 待填写 | 待填写 |
| Legged (Lite3) | 待填写 | 待填写 | 待填写 |

> **指标含义**：
>
> | 指标 | 含义 | 对比意义 |
> | ---- | ---- | -------- |
> | `Path Length` | 沿轨迹的总行驶距离 | 足式可能因稳定性调整而增加路径长度 |
> | `Final Displacement` | 从起点到终点的欧氏位移 | 两平台在相同 waypoint 下的实际到达距离差异 |
> | `Lateral Deviation` | 相对期望方向的平均侧向偏差 | **核心指标** — 越低 = 路径跟踪精度越高 |
>
> **预期观察**：
> - 轮式 `Lat. Dev.` 更低 → 差速驱动路径跟踪更精确
> - 足式 `Lat. Dev.` 更高 → 揭示足式部署的核心挑战
> - 如果差距 >2×，证明 **轮式训练数据直接迁移到足式存在非平凡的性能差距**

---

### Part C：全周实验指标一览表

> 以下表格汇聚所有天的核心定量指标，便于快速综览整周工作成果。

| Day | 实验 | 脚本 | 核心指标 | 预期值 / 判断标准 |
| --- | ---- | ---- | -------- | ----------------- |
| 1 | 数据集检查 | `check_dataset.py` | `ok_count / traj_count` | 100%（全部通过） |
| 1 | 离线推理基线 | `offline_inference.py` | DDPM-10 推理频率 | 10–20 Hz |
| 2 | DDIM 加速 | `ddim_experiment.py` | DDIM-5 加速比 / MSE | ~2× / <0.001 |
| 2 | 实时推理 | `realtime_inference.py` | 平均 FPS (DDIM-5) | **≥15 Hz**（足式门槛） |
| 3 | CFG 引导 | `cfg_experiment.py` | 最佳 w 区间 | w=1.0~2.0 |
| 3 | 步态抖动 | `gait_shake_robustness.py` | cos_sim (行走/小跑) | >0.95 / <0.90 |
| 4 | DINOv2 训练 | `train_dinov2.py` | test_loss vs EfficientNet | 待对比 |
| 5 | 开环仿真 | `lite3_sim.py` | Total Displacement | >0（机器人移动） |
| 5 | 轮足对比 | `lite3_sim.py --compare` | Lat. Dev. 差距 | 足式 > 轮式 |
| 6 | 消融实验 | `ablation_experiment.py` | 最佳配置 cos_sim / Time | >0.94 / <70 ms |

---

### Part D：关键可视化图表索引

| 图表 | 来源脚本 | 输出路径 | 论文对应章节 |
| ---- | -------- | -------- | ------------ |
| 导航 vs 探索轨迹对比 | `offline_inference.py` | `results/day1/` | §3.1 基线验证 |
| DDIM 时间-质量对比 (3 面板) | `ddim_experiment.py` | `results/day2/ddim_comparison.png` | §4.1 DDIM 加速 |
| 实时 FPS 趋势图 | `realtime_inference.py` | `results/realtime/fps_trend.png` | §4.1 实时性验证 |
| CFG 轨迹分布 (2×3 网格) | `cfg_experiment.py` | `results/day3/cfg_comparison.png` | §4.2 CFG 增强 |
| CFG 趋势图 (Diversity/Disp/Time) | `cfg_experiment.py` | `results/day3/cfg_trends.png` | §4.2 CFG 增强 |
| 步态抖动鲁棒性 (轨迹+柱状图) | `gait_shake_robustness.py` | `results/day3/gait_shake_robustness.png` | §4.3 抖动分析 |
| DINOv2 vs EffNet 训练曲线 | wandb 导出 | `results/day4/` | §4.4 编码器对比 |
| 轮式 vs 足式仿真轨迹 | `lite3_sim.py --compare` | `results/day5/` | §4.5 跨形态对比 |
| 消融热力图 | `ablation_experiment.py` | `results/day6/ablation_heatmap.png` | §4.6 消融研究 |
| Pareto 前沿 (速度 vs 质量) | `ablation_experiment.py` | `results/day6/ablation_pareto.png` | §4.6 消融研究 |
| 多维雷达图 (Top 3 配置) | `ablation_experiment.py` | `results/day6/ablation_radar.png` | §4.6 消融研究 |

---

## 核心研究问题与科研空白

### 为什么要把视觉导航算法部署到足式机器人？

**一句话定位**：扩散策略视觉导航（NoMaD/ViNT/GNM）仅在轮式平台上验证，
**从未有人在足式机器人上验证连续 waypoint 扩散策略**——这是一个真实存在且亟需填补的研究空白。

#### 已有工作的形态覆盖（截至 2026 年初）

| 项目 | 策略类型 | 动作空间 | 已验证平台 | 足式验证 |
|------|---------|---------|-----------|----------|
| GNM/ViNT | 回归 | 离散 waypoint | LoCoBot, TurtleBot | ❌ |
| **NoMaD** | **DDPM 扩散** | **连续 8×(x,y) waypoint** | **LoCoBot** | **❌** |
| NaVILA | VLA 大模型 | 离散动作（前进/左转/右转/停） | Go2, G1, H1 | ✅ 但非扩散 |
| InternNav | 扩散编码器 | 离散动作（4 选 1） | Go2, G1 | ✅ 但动作离散 |
| VP-Nav | 经典规划 | 速度指令 | Unitree A1 | ✅ 但非学习策略 |

**关键观察**：InternNav 虽底层用了扩散编码器，但动作空间已退化为 **离散 4 选 1**，
丢失了扩散策略最核心的优势——**多模态连续轨迹采样**。

#### 五个核心科研空白

| 编号 | 空白 | 本周验证方式 |
|------|------|------------|
| G1 | **连续 waypoint 扩散策略从未在足式平台验证** | Day 1 基线 + Day 5 仿真部署 |
| G2 | **轮式数据训练→足式零样本迁移效果未知** | Day 5 轮式 vs 足式对比 |
| G3 | **扩散采样频率能否满足足式控制实时性？** | Day 2 DDIM 加速 → 控制频率分析 |
| G4 | **步态引起的相机抖动对视觉导航的影响未被量化** | Day 3 下午 步态抖动鲁棒性实验 |
| G5 | **视觉编码器对形态差异（视高、视野）的鲁棒性未知** | Day 4 DINOv2 自监督特征对比 |

#### 轮式 → 足式迁移为何非平凡？

| 维度 | 轮式（LoCoBot） | 足式（Lite3） | 对 NoMaD 的挑战 |
|------|-----------------|-------------|----------------|
| 运动学 | 差速驱动 $(v,\omega)$ | 12 DoF 关节空间 | PD 控制器需中间步态层 |
| 相机稳定性 | 平稳无抖动 | 步态周期性俯仰±3°晃动 | EfficientNet 对运动模糊鲁棒性？ |
| 控制频率 | 底盘 10–50 Hz | 关节 100–1000 Hz | 扩散 5–10 Hz 决策是否够？ |
| 稳定性 | 天然稳定 | 需主动平衡 | 急转弯 waypoint 可能致倒伏 |
| 地形 | 仅平面 | 可过台阶/斜坡 | 2D waypoint 无法表达高程 |
| 视角 | 高度≈0.4m 前视 | 高度≈0.3m 可能更宽 FoV | 训练数据视角分布 mismatch |

#### 本周工作的论文叙事线

> **核心论点**：NoMaD 的扩散策略通过 waypoint 抽象层实现了形态无关的视觉导航。
> 我们**首次**在四足机器人上系统验证了这一假说，揭示了轮式→足式迁移中
> [频率适配 / 相机抖动 / 目标引导] 等此前被轮式部署掩盖的关键挑战，
> 并提出了基于 DDIM 加速 + CFG 增强的适配方案。

**论文结构映射**：

```
Day 1 → §3.1 实验基础设施（环境、基线、数据）
Day 2 → §4.1 实验一：DDIM 加速满足足式控制频率
Day 3 → §4.2 实验二：CFG 增强 + §4.3 步态抖动鲁棒性
Day 4 → §4.4 实验三：视觉编码器跨形态泛化
Day 5 → §4.5 实验四：轮式 vs 足式仿真对比
Day 6 → §4.6 消融研究（含足式特有维度）
Day 7 → §5 讨论 + §6 结论
```

---

## Day 1（周一）：环境搭建 + 基线跑通 + 文献空白梳理

### 1.1 创建 Conda 环境（≈ 15 min）

**步骤 1**：新建 Python 3.8 环境

```bash
conda create -n nomad python=3.8 -y
conda activate nomad
```

**步骤 2**：安装 PyTorch（CUDA 12.1，适配 4090）

```bash
# 4090 需要 sm_89 架构，必须使用 CUDA ≥ 11.8
pip install torch==2.1.0 torchvision==0.16.0 --index-url https://download.pytorch.org/whl/cu121
```

> **注意**：项目原始 `train_environment.yml` 中写的 `cudatoolkit=10.` 是旧版配置，
> **不兼容 RTX 4090**。此处已替换为 CUDA 12.1。

**步骤 3**：安装项目核心依赖

```bash
cd /home/yifei/codespace/visualnav-transformer/train
pip install -e .
```

**步骤 4**：安装所有实验依赖（使用统一 requirements）

> 📋 完整的版本锁定清单在 `scripts/requirements.txt`，下面的命令会一次性安装所有依赖。

```bash
cd /home/yifei/codespace/visualnav-transformer
pip install -r scripts/requirements.txt
```

> ✅ **关于 `diffusers` 版本**：
> `requirements.txt` 使用 `diffusers==0.11.1`（与项目原始 `train_environment.yml` 一致），
> 同时锁定 `huggingface-hub==0.10.1`，确保 `cached_download` 兼容。
> `DDPMScheduler` / `DDIMScheduler` 均在 0.11.1 中可用（自 0.2.0 版本起就有）。

<details>
<summary>📦 关键包版本速查表（点击展开）</summary>

| 包名 | 版本 | 说明 |
|------|------|------|
| `torch` | 2.1.0+cu121 | 步骤 2 已安装（4090 必须 CUDA ≥ 11.8） |
| `torchvision` | 0.16.0+cu121 | 与 torch 2.1.0 匹配 |
| `diffusers` | **0.11.1** | 与项目原始配置一致 |
| `huggingface-hub` | **0.10.1** | 锁定以保留 `cached_download` 兼容 |
| `efficientnet-pytorch` | ≥1.1.0 | NoMaD 视觉编码器 |
| `vit-pytorch` | ≥0.40.0 | Vision Transformer 组件 |
| `positional-encodings` | ≥6.0.0 | 时序位置编码 |
| `warmup-scheduler` | ≥0.3.0 | 学习率预热 |
| `wandb` | 0.12.18 | 实验日志 |
| `opencv-python` | 4.6.0.66 | 图像处理 |
| `h5py` | 3.6.0 | 数据存储 |
| `tqdm` | 4.64.0 | 进度条 |
| `numpy` | ≥1.21, <2.0 | 避免 numpy 2.0 破坏性变更 |
| `matplotlib` | ≥3.5 | 可视化 |
| `timm` | ≥0.9.0 | Day 4 DINOv2 需要 |

</details>

**步骤 5**：确认 `diffusion_policy` 可用

NoMaD 的 `ConditionalUnet1D` 来自 `diffusion_policy`。

```bash
# 检查项目中是否已包含
ls /home/yifei/codespace/visualnav-transformer/train/diffusion_policy/

# 若目录不存在，需手动克隆并放入
git clone https://github.com/real-stanford/diffusion_policy.git /tmp/dp
cp -r /tmp/dp/diffusion_policy /home/yifei/codespace/visualnav-transformer/train/
```

**步骤 6**：验证安装

```bash
cd /home/yifei/codespace/visualnav-transformer/train
python -c "import torch; print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0))"
python -c "from vint_train.models.nomad.nomad import NoMaD; print('NoMaD import OK')"
python -c "from diffusion_policy.model.diffusion.conditional_unet1d import ConditionalUnet1D; print('UNet1D import OK')"
```

预期输出：

```
True
NVIDIA GeForce RTX 4090
NoMaD import OK
UNet1D import OK
```

---

### 1.2 训练输出权重说明（≈ 10 min 阅读）

> **GitHub Release 未提供 `nomad.pth`**，需要自行训练。以下说明训练产出的文件结构
> 以及如何选取最终权重用于后续推理实验。

#### 训练输出目录

运行 `python train.py -c config/nomad.yaml` 后，权重保存在：

```
logs/nomad/nomad_<时间戳>/
```

时间戳格式为 `YYYY_MM_DD_HH_MM_SS`，例如 `logs/nomad/nomad_2026_03_25_14_30_00/`。

#### 目录中的文件

| 文件名 | 内容 | 每轮行为 | 说明 |
|--------|------|---------|------|
| `config.yaml` | 本次训练的完整配置 | 训练开始时保存一次 | 便于复现 |
| `latest.pth` | **模型 `state_dict`** | 每轮覆盖 | 最后一轮的权重 |
| `{epoch}.pth` | **模型 `state_dict`** | 每轮新建 | 如 `0.pth`, `1.pth`, ... `99.pth` |
| `ema_{epoch}.pth` | **EMA 模型 `state_dict`** | 每轮新建 | 指数滑动平均权重，通常更稳定 |
| `optimizer_latest.pth` | 优化器 `state_dict` | 每轮覆盖 | 恢复训练用 |
| `scheduler_latest.pth` | 学习率调度器 `state_dict` | 每轮覆盖 | 恢复训练用 |

> 📝 **注意**：NoMaD 训练保存的是**纯 `state_dict`**（不是整个模型对象），
> 因此加载时直接 `model.load_state_dict(torch.load(...))` 即可。

#### 如何选取最终权重

推荐优先使用 **EMA 权重** `ema_{epoch}.pth`，因为 EMA（指数滑动平均）
能平滑训练过程中的权重振荡，通常泛化性能更好。

**选择策略**：

1. **观察 wandb 曲线**：找到 `avg_total_test_loss` 最低的 epoch 编号 `N`
2. **使用对应 EMA 权重**：`ema_N.pth`
3. **如果不想挑选**：直接使用最后一轮的 EMA 权重，即最大编号的 `ema_*.pth`

**步骤**：将选定的权重复制到推理脚本期望的路径：

```bash
# 假设 epoch 87 的 test loss 最低
TRAIN_RUN="logs/nomad/nomad_2026_03_25_14_30_00"  # ← 替换为你的实际目录名

mkdir -p /home/yifei/codespace/visualnav-transformer/deployment/model_weights/nomad
cp /home/yifei/codespace/visualnav-transformer/train/${TRAIN_RUN}/ema_87.pth \
   /home/yifei/codespace/visualnav-transformer/deployment/model_weights/nomad/nomad.pth
```

**确认文件就位**：

```bash
ls -lh /home/yifei/codespace/visualnav-transformer/deployment/model_weights/nomad/nomad.pth
# 应显示约 200MB 的文件
```

---

### 1.3 检查数据集完整性（≈ 10 min）

每条轨迹文件夹（如 `no10vc_10_0`）应包含：
- `traj_data.pkl` — 轨迹元数据（position、yaw）
- 编号的 `.jpg` 图片（直接位于轨迹文件夹内）

**创建检查脚本**：`scripts/check_dataset.py`

```python
#!/usr/bin/env python3
"""Day 1: 数据集完整性检查"""

import os
import pickle
import glob
from datetime import datetime

DATASET_ROOT = "/home/yifei/codespace/visualnav-transformer/nomad_dataset/go_stanford"
PROJECT_ROOT = "/home/yifei/codespace/visualnav-transformer"
RUN_TAG = datetime.now().strftime("%Y%m%d_%H%M%S")

trajs = sorted(os.listdir(DATASET_ROOT))
print(f"Found {len(trajs)} trajectories")

records = []  # 收集每条轨迹的检查结果
for traj in trajs:
    traj_path = os.path.join(DATASET_ROOT, traj)
    pkl_path  = os.path.join(traj_path, "traj_data.pkl")

    if not os.path.exists(pkl_path):
        print(f"  [MISSING] {traj}: no traj_data.pkl")
        records.append((traj, "MISSING", 0, 0, 0))
        continue

    with open(pkl_path, "rb") as f:
        data = pickle.load(f)

    n_pos    = len(data.get("position", []))
    n_yaw    = len(data.get("yaw", []))
    n_images = len(glob.glob(os.path.join(traj_path, "*.jpg")))

    print(f"  [OK] {traj}: {n_pos} positions, {n_yaw} yaws, {n_images} images")
    records.append((traj, "OK", n_pos, n_yaw, n_images))

ok_count  = sum(1 for r in records if r[1] == "OK")
bad_count = len(records) - ok_count
print(f"\nCheck complete: {ok_count} OK, {bad_count} errors")

# ── 保存 txt 结果文件 ───────────────────────────────
out_dir = os.path.join(PROJECT_ROOT, f"results/day1/{RUN_TAG}_check_dataset")
os.makedirs(out_dir, exist_ok=True)
txt_path = os.path.join(out_dir, "check_results.txt")
with open(txt_path, "w") as f:
    f.write("# Dataset Integrity Check Results\n")
    f.write(f"# Run: {RUN_TAG}\n")
    f.write(f"# Dataset: {DATASET_ROOT}\n")
    f.write("# 列说明:\n")
    f.write("#   traj_name - 轨迹子目录名\n")
    f.write("#   status    - OK / MISSING (是否存在 traj_data.pkl)\n")
    f.write("#   n_pos     - position 序列长度\n")
    f.write("#   n_yaw     - yaw 序列长度\n")
    f.write("#   n_images  - jpg 图片数量\n")
    f.write("\n")
    f.write(f"{'traj_name':<24}{'status':<10}{'n_pos':<8}{'n_yaw':<8}{'n_images':<10}\n")
    f.write("-" * 60 + "\n")
    for traj, status, n_pos, n_yaw, n_images in records:
        f.write(f"{traj:<24}{status:<10}{n_pos:<8}{n_yaw:<8}{n_images:<10}\n")
    f.write(f"\n# Summary: {ok_count} OK, {bad_count} errors out of {len(records)} total\n")
print(f"[SAVED] {txt_path}")
```

> **📁 文件放置**：将上述代码保存到远程训练机的  
> `/home/yifei/codespace/visualnav-transformer/scripts/check_dataset.py`  
> **✅ 运行方式**：

```bash
cd /home/yifei/codespace/visualnav-transformer
python scripts/check_dataset.py
```

---

### 1.4 编写离线推理脚本（≈ 1.5 h）

> 这是 Day 1 **最核心**的步骤：脱离 ROS，用纯 Python 跑通 NoMaD 推理。

#### 推理流程总览

```
输入: 3 帧历史观察 + 1 帧目标图像
  ↓  视觉编码器 (NoMaD_ViNT)
  ↓  256 维条件向量
  ↓  距离预测网络 → 预测到目标距离
  ↓  扩散策略网络 (DDPM, 10 步去噪)
输出: 8 个 (x, y) waypoint
```

#### 创建脚本：`scripts/offline_inference.py`

```python
#!/usr/bin/env python3
"""
Day 1: NoMaD 离线推理基线
在数据集图像上运行 DDPM 推理（无需 ROS），并可视化生成轨迹。
"""

import os
import sys
import glob
import time
from datetime import datetime

import numpy as np
import torch
import matplotlib.pyplot as plt
from PIL import Image as PILImage
from torchvision import transforms

# ── 运行时间戳（时间戳 + 脚本名）─────────────────────
RUN_TAG = datetime.now().strftime("%Y%m%d_%H%M%S")

# ── Project Paths ─────────────────────────────────────────
PROJECT_ROOT = "/home/yifei/codespace/visualnav-transformer"
sys.path.insert(0, os.path.join(PROJECT_ROOT, "train"))
sys.path.insert(0, os.path.join(PROJECT_ROOT, "deployment/src"))

from vint_train.models.nomad.nomad import NoMaD, DenseNetwork
from vint_train.models.nomad.nomad_vint import NoMaD_ViNT, replace_bn_with_gn
from diffusion_policy.model.diffusion.conditional_unet1d import ConditionalUnet1D
from diffusers.schedulers.scheduling_ddpm import DDPMScheduler

# ── Configuration ──────────────────────────────────────────────
# Self-trained weights: copy ema_N.pth from logs/ to this path (see §1.2)
MODEL_WEIGHTS = os.path.join(
    PROJECT_ROOT, "deployment/model_weights/nomad/nomad.pth"
)
DATASET_ROOT = os.path.join(PROJECT_ROOT, "nomad_dataset/go_stanford")

IMAGE_SIZE        = (96, 96)   # (宽, 高)
CONTEXT_SIZE      = 3          # 历史帧数
LEN_TRAJ_PRED     = 8          # 预测路径点数
NUM_DIFFUSION_ITERS = 10       # DDPM 去噪步数
NUM_SAMPLES       = 8          # 并行采样数
ENCODING_SIZE     = 256        # 视觉特征维度


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#  模型构建 —— 对应 deployment/src/utils.py → load_model()
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
def build_model(device: torch.device) -> NoMaD:
    """按照 utils.py load_model 的逻辑构建并加载 NoMaD"""
    print("Building model...")

    # 1) Vision Encoder
    vision_encoder = NoMaD_ViNT(
        obs_encoding_size=ENCODING_SIZE,
        context_size=CONTEXT_SIZE,
        mha_num_attention_heads=4,
        mha_num_attention_layers=4,
        mha_ff_dim_factor=4,
    )
    vision_encoder = replace_bn_with_gn(vision_encoder)

    # 2) 噪声预测网络 (Conditional UNet1D)
    noise_pred_net = ConditionalUnet1D(
        input_dim=2,                     # 每个路径点是 (x, y)
        global_cond_dim=ENCODING_SIZE,   # 条件向量维度
        down_dims=[64, 128, 256],        # 来自 nomad.yaml
        cond_predict_scale=False,        # 来自 nomad.yaml
    )

    # 3) 距离预测网络
    dist_pred_net = DenseNetwork(embedding_dim=ENCODING_SIZE)

    # 4) 组装模型
    model = NoMaD(
        vision_encoder=vision_encoder,
        noise_pred_net=noise_pred_net,
        dist_pred_net=dist_pred_net,
    )

    # 5) 加载预训练权重
    print(f"Loading weights: {MODEL_WEIGHTS}")
    state_dict = torch.load(MODEL_WEIGHTS, map_location=device)
    model.load_state_dict(state_dict, strict=False)
    model.to(device).eval()
    print("Model loaded successfully!")
    return model


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#  图像处理
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
_TRANSFORM = transforms.Compose([
    transforms.ToTensor(),
    transforms.Normalize(
        mean=[0.485, 0.456, 0.406],
        std=[0.229, 0.224, 0.225],
    ),
])


def _find_image(traj_path: str, idx: int) -> str:
    """在轨迹目录中查找第 idx 帧图像（支持 .jpg / .png）"""
    for ext in (".jpg", ".png", ".jpeg"):
        p = os.path.join(traj_path, f"{idx}{ext}")
        if os.path.exists(p):
            return p
    raise FileNotFoundError(f"Cannot find frame {idx} image: {traj_path}")


def load_and_transform(image_path: str) -> torch.Tensor:
    """加载单张图像 → [3, 96, 96] 归一化张量"""
    img = PILImage.open(image_path).convert("RGB").resize(IMAGE_SIZE)
    return _TRANSFORM(img)


def prepare_observation(traj_path: str, t: int) -> torch.Tensor:
    """
    准备观测输入: 取 [t-CONTEXT_SIZE, ..., t] = CONTEXT_SIZE+1 帧，
    拼接为 [1, (CONTEXT_SIZE+1)*3, H, W]。
    """
    frames = []
    for i in range(max(0, t - CONTEXT_SIZE), t + 1):
        frames.append(load_and_transform(_find_image(traj_path, i)))

    # 帧数不足时用最早的帧填充
    while len(frames) < CONTEXT_SIZE + 1:
        frames.insert(0, frames[0].clone())

    frames = frames[-(CONTEXT_SIZE + 1):]      # 只保留最近 C+1 帧
    return torch.cat(frames, dim=0).unsqueeze(0)  # [1, 12, 96, 96]


def prepare_goal(traj_path: str, t: int) -> torch.Tensor:
    """准备目标图像 → [1, 3, 96, 96]"""
    return load_and_transform(_find_image(traj_path, t)).unsqueeze(0)


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#  DDPM 扩散推理
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
def diffusion_inference(
    model: NoMaD,
    obs_cond: torch.Tensor,   # [1, 256]
    device: torch.device,
    num_samples: int = NUM_SAMPLES,
    num_steps: int = NUM_DIFFUSION_ITERS,
) -> torch.Tensor:
    """
    通过 DDPM 反向去噪从高斯噪声生成路径点轨迹。
    返回: [num_samples, LEN_TRAJ_PRED, 2]
    """
    scheduler = DDPMScheduler(
        num_train_timesteps=num_steps,
        beta_schedule="squaredcos_cap_v2",
        clip_sample=True,
        prediction_type="epsilon",
    )

    cond = obs_cond.repeat(num_samples, 1)            # [N, 256]
    naction = torch.randn(
        (num_samples, LEN_TRAJ_PRED, 2), device=device
    )

    scheduler.set_timesteps(num_steps)
    with torch.no_grad():
        for k in scheduler.timesteps:
            noise_pred = model(
                "noise_pred_net",
                sample=naction,
                timestep=k,
                global_cond=cond,
            )
            naction = scheduler.step(
                model_output=noise_pred,
                timestep=k,
                sample=naction,
            ).prev_sample

    return naction                                     # [N, 8, 2]


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#  可视化
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
def visualize_trajectories(
    obs_path: str,
    goal_path: str,
    trajectories: torch.Tensor,
    save_path: str,
):
    fig, axes = plt.subplots(1, 3, figsize=(18, 6))

    axes[0].imshow(PILImage.open(obs_path))
    axes[0].set_title("Observation (current)", fontsize=14)
    axes[0].axis("off")

    axes[1].imshow(PILImage.open(goal_path))
    axes[1].set_title("Goal", fontsize=14)
    axes[1].axis("off")

    trajs = trajectories.cpu().numpy()
    for i in range(trajs.shape[0]):
        t = trajs[i]
        axes[2].plot(
            t[:, 1], t[:, 0], "o-", alpha=0.5, markersize=3,
            label=f"sample {i}" if i < 4 else None,
        )
    axes[2].plot(0, 0, "r*", markersize=15, label="robot")
    axes[2].set_title(f"Generated Trajectories (n={trajs.shape[0]})")
    axes[2].set_xlabel("y (lateral)")
    axes[2].set_ylabel("x (forward)")
    axes[2].legend(fontsize=8)
    axes[2].set_aspect("equal")
    axes[2].grid(True, alpha=0.3)

    plt.tight_layout()
    os.makedirs(os.path.dirname(save_path), exist_ok=True)
    plt.savefig(save_path, dpi=150, bbox_inches="tight")
    plt.close()
    print(f"Visualization saved: {save_path}")


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#  主流程
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
def main():
    device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
    print(f"Device: {device}")

    # 1. 构建并加载模型
    model = build_model(device)

    # 2. 选择一条轨迹
    traj_names = sorted(os.listdir(DATASET_ROOT))
    traj_path  = os.path.join(DATASET_ROOT, traj_names[0])
    print(f"\nTest trajectory: {traj_names[0]}")

    images = sorted(glob.glob(os.path.join(traj_path, "*.jpg")))
    if not images:
        images = sorted(glob.glob(os.path.join(traj_path, "*.png")))
    n_images = len(images)
    print(f"Trajectory contains {n_images} frames")

    # 3. 设置观测/目标时刻
    obs_time  = CONTEXT_SIZE + 2
    goal_time = min(obs_time + 15, n_images - 1)
    print(f"Observation frame: t={obs_time}, Goal frame: t={goal_time}")

    # 4. 准备输入张量
    obs_img  = prepare_observation(traj_path, obs_time).to(device)
    goal_img = prepare_goal(traj_path, goal_time).to(device)

    # 5. 视觉编码（导航模式 mask=0）
    print("\n--- Vision Encoding ---")
    mask = torch.zeros(1).long().to(device)
    with torch.no_grad():
        obsgoal_cond = model(
            "vision_encoder",
            obs_img=obs_img,
            goal_img=goal_img,
            input_goal_mask=mask,
        )
    print(f"Condition vector shape: {obsgoal_cond.shape}")   # → [1, 256]

    # 6. 距离预测
    print("\n--- Distance Prediction ---")
    with torch.no_grad():
        dist = model("dist_pred_net", obsgoal_cond=obsgoal_cond)
    print(f"Predicted distance: {dist.item():.4f}")

    # 7. DDPM 扩散推理
    print("\n--- Diffusion Inference (DDPM, 10 steps) ---")
    t0 = time.time()
    trajectories = diffusion_inference(model, obsgoal_cond, device)
    elapsed = time.time() - t0
    print(f"Inference time: {elapsed:.4f} s")
    print(f"Trajectory shape: {trajectories.shape}")       # → [8, 8, 2]

    # 8. 可视化（时间戳_脚本名 输出目录）
    result_dir = os.path.join(PROJECT_ROOT, f"results/day1/{RUN_TAG}_offline_inference")
    os.makedirs(result_dir, exist_ok=True)
    obs_img_path  = _find_image(traj_path, obs_time)
    goal_img_path = _find_image(traj_path, goal_time)

    visualize_trajectories(
        obs_img_path, goal_img_path, trajectories,
        os.path.join(result_dir, "baseline_trajectories.png"),
    )

    # 9. 探索模式对比 (mask=1)
    print("\n--- Exploration Mode (mask=1) ---")
    mask_explore = torch.ones(1).long().to(device)
    with torch.no_grad():
        explore_cond = model(
            "vision_encoder",
            obs_img=obs_img,
            goal_img=goal_img,
            input_goal_mask=mask_explore,
        )
    explore_trajs = diffusion_inference(model, explore_cond, device)
    visualize_trajectories(
        obs_img_path, goal_img_path, explore_trajs,
        os.path.join(result_dir, "explore_trajectories.png"),
    )

    # 10. 保存运行摘要到 txt
    summary_path = os.path.join(result_dir, "summary.txt")
    n_params = sum(p.numel() for p in model.parameters())
    lines = [
        f"Run time: {RUN_TAG}",
        f"Script: offline_inference.py",
        f"Test trajectory: {traj_names[0]}",
        f"Model params: {n_params:,}",
        f"DDPM inference time: {elapsed:.4f} s ({NUM_DIFFUSION_ITERS} steps)",
        f"Inference frequency: {1.0/elapsed:.1f} Hz",
        f"Generated trajectories: {NUM_SAMPLES} x {LEN_TRAJ_PRED} waypoints",
        f"Predicted distance: {dist.item():.4f}",
    ]
    with open(summary_path, "w") as f:
        f.write("\n".join(lines))
    print(f"\nSummary saved: {summary_path}")

    # 11. 打印摘要
    print("\n" + "=" * 60)
    print("Day 1 baseline verification complete!")
    for l in lines:
        print(f"  {l}")
    print(f"  Results directory: {result_dir}")
    print("=" * 60)


if __name__ == "__main__":
    main()
```

#### 运行 & 检查

> **📁 文件放置**：将上述代码保存到远程训练机的  
> `/home/yifei/codespace/visualnav-transformer/scripts/offline_inference.py`  
> **✅ 运行方式**：

```bash
cd /home/yifei/codespace/visualnav-transformer
python scripts/offline_inference.py
```

预期输出：
- 模型参数量 ≈ 50 M
- DDPM 推理时间 ≈ 0.05–0.1 s（4090）
- `results/day1/` 下生成两张对比图：
  - `baseline_trajectories.png` — 导航轨迹（应朝向目标）
  - `explore_trajectories.png` — 探索轨迹（应更分散）

**📊 指标输出说明**（对应 `summary.txt`）：

| 指标 | 含义 | 如何解读 |
| ---- | ---- | -------- |
| `DDPM inference time` | 10 步去噪的总推理延迟 | 除以 1 得推理频率；轮式 ≥4 Hz 即可，足式需 ≥15 Hz |
| `Inference frequency` | 推理频率 = 1 / inference_time | 是否满足部署目标的直接判据 |
| `Predicted distance` | 模型预测观测帧到目标帧的距离（归一化） | 越小 = 模型认为越接近目标 |
| `Generated trajectories` | N × M waypoints (如 8×8) | N=并行采样数，M=路径点数；多样性体现在 N 条轨迹的分散程度 |

---

### 1.5 文献空白梳理（≈ 1 h，与环境安装并行）

在等待安装和下载时，完成以下文献分析工作：

**任务**：在 `文档/科研空白分析.md` 中整理以下表格：

| 论文 | 策略类型 | 动作空间 | 是否足式 | 关键限制 |
|------|---------|---------|---------|--------|
| GNM (Shah 2023) | 回归 | 离散 | ❌ 轮式 | 无扩散策略 |
| ViNT (Shah 2023) | 回归 | 离散 | ❌ 轮式 | 无多模态采样 |
| NoMaD (Sridhar 2024) | DDPM | 连续 8×2 | ❌ 轮式 | **本文基线** |
| NaVILA (Cheng 2025) | VLA | 离散 4 选 1 | ✅ Go2/G1 | 非扩散，动作离散 |
| InternNav (2025) | 扩散编码 | 离散 4 选 1 | ✅ Go2 | 扩散仅用于编码 |
| VP-Nav (Shah 2022) | 经典规划 | 速度指令 | ✅ A1 | 非学习策略 |

**要回答的问题**：
1. 是否有工作在足式平台使用**连续 waypoint 扩散策略**？→ 否，这是空白
2. 轮式训练数据能否直接迁移到足式？→ GNM 验证了轮式跨型号，但未验证足式
3. 足式部署面临哪些轮式不存在的挑战？→ 控制频率、步态抖动、稳定性约束

---

### 1.6 Day 1 验收清单

- [ ] Conda 环境创建成功，所有 `import` 无报错
- [ ] 已了解训练输出文件结构（`latest.pth` / `ema_{epoch}.pth`）
- [ ] 已将自训练权重复制到 `deployment/model_weights/nomad/nomad.pth`
- [ ] 数据集检查通过
- [ ] 离线推理脚本跑通
- [ ] 生成了导航 / 探索两种模式的轨迹可视化图
- [ ] 记录了 DDPM 基线推理时间
- [ ] 完成科研空白分析表格，明确论文定位

---

## Day 2（周二）：DDIM 加速采样——满足足式控制频率需求

### 2.1 研究动机：为什么需要 DDIM 加速？

> **科研空白 G3**：扩散采样频率能否满足足式控制实时性？

| 场景 | 控制频率需求 | NoMaD DDPM-10 步 | 问题 |
|------|-------------|------------------|------|
| 轮式底盘 | 4–10 Hz | ≈5–10 Hz | 勉强匹配 |
| 四足高层导航 | 10–30 Hz | ≈5–10 Hz | **有延迟风险** |
| 四足关节控制 | 100–1000 Hz | — | 由步态控制器处理 |

对于足式机器人，DDIM 加速不仅是性能优化，而是**部署必要条件**：
如果能将扩散采样从 10 步压缩到 3–5 步且保持轨迹质量，
决策频率可提升至 15–25 Hz，满足四足导航层的实时性要求。

### 2.2 理解 DDIM 原理（≈ 30 min）

| | DDPM | DDIM |
|---|---|---|
| 去噪过程 | 随机（每步加噪声） | 确定性（可跳步） |
| 最少步数 | 必须走完全部 T 步 | 可用远少于 T 的步数 |
| 质量 | 基准 | 接近 DDPM |

> 在 `diffusers` 中切换只需一行：`DDPMScheduler` → `DDIMScheduler`。

### 2.2 确认 DDIM 可用

> `diffusers==0.11.1` **已经包含** `DDIMScheduler`（自 0.2.0 版本起就有）。
> 如果你在 Day 1 步骤 4 通过 `scripts/requirements.txt` 安装了 `diffusers==0.11.1` + `huggingface-hub==0.10.1`，
> 两者均可正常导入，无需额外操作。

**快速验证**：

```bash
python -c "from diffusers.schedulers.scheduling_ddim import DDIMScheduler; print('DDIMScheduler OK')"
python -c "from diffusers.schedulers.scheduling_ddpm import DDPMScheduler; print('DDPMScheduler OK')"
```

### 2.3 编写 DDIM 对比脚本

创建 `scripts/ddim_experiment.py`：

```python
#!/usr/bin/env python3
"""
Day 2: DDIM 加速采样对比实验
对比 DDPM-10 步 vs DDIM-{10,5,3,2,1} 步的推理时间、轨迹质量和多样性。
"""

import os
import sys
import time
from datetime import datetime

import numpy as np
import torch
import matplotlib.pyplot as plt

RUN_TAG = datetime.now().strftime("%Y%m%d_%H%M%S")

PROJECT_ROOT = "/home/yifei/codespace/visualnav-transformer"
sys.path.insert(0, os.path.join(PROJECT_ROOT, "train"))
sys.path.insert(0, os.path.join(PROJECT_ROOT, "deployment/src"))
sys.path.insert(0, os.path.join(PROJECT_ROOT, "scripts"))

from diffusers.schedulers.scheduling_ddpm import DDPMScheduler
from diffusers.schedulers.scheduling_ddim import DDIMScheduler

from offline_inference import (
    build_model, prepare_observation, prepare_goal,
    DATASET_ROOT, CONTEXT_SIZE, IMAGE_SIZE, LEN_TRAJ_PRED,
)

NUM_RUNS = 10   # 每个配置重复次数（用于求均值）


def inference_with_scheduler(model, obs_cond, device, scheduler,
                             num_samples=8, num_steps=10):
    """通用扩散推理，支持任意调度器和步数"""
    cond = obs_cond.repeat(num_samples, 1)
    naction = torch.randn((num_samples, LEN_TRAJ_PRED, 2), device=device)

    scheduler.set_timesteps(num_steps)
    with torch.no_grad():
        for k in scheduler.timesteps:
            noise_pred = model(
                "noise_pred_net", sample=naction,
                timestep=k, global_cond=cond,
            )
            naction = scheduler.step(
                model_output=noise_pred, timestep=k, sample=naction,
            ).prev_sample
    return naction


def make_scheduler(kind: str, num_train_timesteps: int = 10):
    common = dict(
        num_train_timesteps=num_train_timesteps,
        beta_schedule="squaredcos_cap_v2",
        clip_sample=True,
        prediction_type="epsilon",
    )
    if kind == "ddpm":
        return DDPMScheduler(**common)
    return DDIMScheduler(**common)


def main():
    device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
    model  = build_model(device)

    # 准备测试数据
    trajs     = sorted(os.listdir(DATASET_ROOT))
    traj_path = os.path.join(DATASET_ROOT, trajs[0])
    obs_time  = CONTEXT_SIZE + 2
    goal_time = obs_time + 15

    obs_img  = prepare_observation(traj_path, obs_time).to(device)
    goal_img = prepare_goal(traj_path, goal_time).to(device)

    mask = torch.zeros(1).long().to(device)
    with torch.no_grad():
        obsgoal_cond = model(
            "vision_encoder", obs_img=obs_img,
            goal_img=goal_img, input_goal_mask=mask,
        )

    # ── Experiment Matrix ──────────────────────────────────────────
    experiments = [
        ("DDPM-10 (baseline)", "ddpm", 10),
        ("DDIM-10",            "ddim", 10),
        ("DDIM-5",             "ddim", 5),
        ("DDIM-3",             "ddim", 3),
        ("DDIM-2",             "ddim", 2),
        ("DDIM-1",             "ddim", 1),
    ]

    # 基线轨迹（用于 MSE 偏差计算）
    baseline_trajs = inference_with_scheduler(
        model, obsgoal_cond, device,
        make_scheduler("ddpm"), num_samples=8, num_steps=10,
    )
    baseline_mean = baseline_trajs.mean(dim=0).cpu().numpy()

    results = {}
    print("=" * 70)
    print("DDIM Accelerated Sampling Experiment")
    print("=" * 70)

    for name, kind, steps in experiments:
        print(f"\n--- {name} ---")
        scheduler = make_scheduler(kind)

        times, all_trajs = [], []
        for _ in range(NUM_RUNS):
            t0 = time.time()
            out = inference_with_scheduler(
                model, obsgoal_cond, device, scheduler,
                num_samples=8, num_steps=steps,
            )
            times.append(time.time() - t0)
            all_trajs.append(out.cpu().numpy())

        all_trajs = np.array(all_trajs)              # [runs, 8, 8, 2]
        mean_traj = all_trajs.mean(axis=(0, 1))      # [8, 2]
        mse       = np.mean((mean_traj - baseline_mean) ** 2)
        diversity = all_trajs.std(axis=(0, 1)).mean()

        results[name] = dict(
            avg_ms=np.mean(times) * 1000,
            std_ms=np.std(times) * 1000,
            mse=mse, diversity=diversity,
            mean_traj=mean_traj, steps=steps,
        )
        print(f"  Time: {results[name]['avg_ms']:.2f} ± {results[name]['std_ms']:.2f} ms")
        print(f"  MSE:  {mse:.6f}    Diversity: {diversity:.4f}")

    # ── Visualization ─────────────────────────────────────
    out_dir = os.path.join(PROJECT_ROOT, "results/day2")
    os.makedirs(out_dir, exist_ok=True)

    fig, axes = plt.subplots(1, 3, figsize=(18, 5))
    names  = list(results.keys())
    colors = ["#e74c3c" if "DDPM" in n else "#3498db" for n in names]

    # 图表 1: 推理时间
    axes[0].bar(range(len(names)),
                [results[n]["avg_ms"] for n in names], color=colors)
    axes[0].set_xticks(range(len(names)))
    axes[0].set_xticklabels(names, rotation=30, ha="right", fontsize=8)
    axes[0].set_ylabel("Inference Time (ms)")
    axes[0].set_title("Inference Time Comparison")

    # 图表 2: 相对基线的 MSE
    axes[1].bar(range(len(names)),
                [results[n]["mse"] for n in names], color=colors)
    axes[1].set_xticks(range(len(names)))
    axes[1].set_xticklabels(names, rotation=30, ha="right", fontsize=8)
    axes[1].set_ylabel("MSE vs Baseline")
    axes[1].set_title("Trajectory Quality Deviation (lower is better)")

    # 图表 3: 轨迹可视化
    for n in names:
        tr = results[n]["mean_traj"]
        ls = "--" if "DDPM" in n else "-"
        axes[2].plot(tr[:, 1], tr[:, 0], ls + "o", label=n,
                     markersize=3, alpha=0.7)
    axes[2].plot(0, 0, "r*", markersize=15)
    axes[2].legend(fontsize=7)
    axes[2].set_title("Mean Trajectory Comparison")
    axes[2].set_xlabel("y")
    axes[2].set_ylabel("x")
    axes[2].set_aspect("equal")
    axes[2].grid(True, alpha=0.3)

    plt.tight_layout()
    plt.savefig(os.path.join(out_dir, "ddim_comparison.png"), dpi=150)
    plt.close()

    # ── Summary Table ─────────────────────────────────────
    print("\n" + "=" * 70)
    print(f"{'Config':<22} {'Time(ms)':<14} {'MSE':<14} {'Diversity':<10}")
    print("-" * 70)
    for n in names:
        r = results[n]
        print(f"{n:<22} {r['avg_ms']:<14.2f} {r['mse']:<14.6f} {r['diversity']:<10.4f}")
    print("=" * 70)

    # ── 保存 txt 结果文件 ───────────────────────────────
    txt_path = os.path.join(out_dir, "results.txt")
    with open(txt_path, "w") as f:
        f.write("# DDIM Acceleration Experiment Results\n")
        f.write(f"# Run: {RUN_TAG}\n")
        f.write("# 列说明:\n")
        f.write("#   Config      - 采样器名称 (如 DDPM-T10, DDIM-S5)\n")
        f.write("#   Time(ms)    - 单次推理平均延迟\n")
        f.write("#   Std(ms)     - 延迟标准差\n")
        f.write("#   MSE         - 与 DDPM 基线轨迹的均方误差\n")
        f.write("#   Diversity   - 采样轨迹端点标准差 (越高=越多样)\n")
        f.write("#   Steps       - 使用的去噪步数\n")
        f.write("#   Speedup     - 相对 DDPM 基线的加速比\n")
        f.write("\n")
        header = f"{'Config':<22}{'Time(ms)':<12}{'Std(ms)':<12}{'MSE':<14}{'Diversity':<12}{'Steps':<8}{'Speedup':<10}\n"
        f.write(header)
        f.write("-" * len(header) + "\n")
        base_t = results[names[0]]["avg_ms"] if names else 1.0
        for n in names:
            r = results[n]
            speedup = base_t / r["avg_ms"] if r["avg_ms"] > 0 else 0
            f.write(f"{n:<22}{r['avg_ms']:<12.2f}{r.get('std_ms',0):<12.2f}"
                    f"{r['mse']:<14.6f}{r['diversity']:<12.4f}"
                    f"{r.get('steps','?'):<8}{speedup:<10.2f}\n")
        # 自动标记最佳配置
        best = min(names, key=lambda n: results[n]["avg_ms"])
        f.write(f"\n# Best speed: {best} ({results[best]['avg_ms']:.2f} ms)\n")
    print(f"\n[SAVED] {txt_path}")


if __name__ == "__main__":
    main()
```

#### 运行

> **📁 文件放置**：将上述代码保存到远程训练机的  
> `/home/yifei/codespace/visualnav-transformer/scripts/ddim_experiment.py`  
> **前置条件**：Day 1 步骤 4 已安装 `scripts/requirements.txt`（含 `diffusers==0.11.1`）  
> **✅ 运行方式**：

```bash
cd /home/yifei/codespace/visualnav-transformer
python scripts/ddim_experiment.py
```

**📊 指标输出说明**（对应终端 Summary Table + `results/day2/results.txt`）：

| 指标 | 含义 | 足式部署判断 |
| ---- | ---- | ------------ |
| `Time (ms)` | 单次推理平均延迟（包含调度器循环） | **≤67 ms → ≥15 Hz**，满足四足高层导航 |
| `MSE` | 与 DDPM-10 基线平均轨迹的均方误差 | <0.001 为可接受质量损失 |
| `Diversity` | 采样轨迹端点标准差 | 反映多模态采样能力；过低则扩散策略优势丧失 |
| `Speedup` | 相对 DDPM-10 的加速倍数 | DDIM-5 预期 ~2×，DDIM-3 预期 ~3× |

> 💡 **决策指导**：选取 `MSE < 0.001` 且 `Speedup ≥ 2×` 的最佳配置作为后续所有实验的默认采样器。

---

### 2.4 实时摄像头推理（≈ 1.5 h）

> **研究价值**：将 NoMaD 从离线数据集推理延伸到**实时视频流**，验证模型在真实感知环境中的
> 决策频率和响应能力。这是足式部署前的**必要中间步骤**——先用摄像头验证实时性，再对接仿真/真机。

#### 实时推理流水线

```
USB 摄像头 (/dev/video0)
  → OpenCV 采集帧
  → 维护 context_size=3 的滑动窗口
  → 每隔 N 帧选取目标帧（最远帧 / 手动指定）
  → NoMaD 视觉编码 + DDIM-5 步扩散推理
  → 8 × (x,y) waypoint 叠加绘制到画面
  → 实时显示 + 录像保存
```

#### 创建脚本：`scripts/realtime_inference.py`

```python
#!/usr/bin/env python3
"""
Day 2.4: 实时摄像头推理
从 Ubuntu 系统摄像头采集画面，实时运行 NoMaD + DDIM 推理，
将预测路径点轨迹叠加到画面上并保存为视频。

功能:
  1. USB 摄像头实时采集 (640x480 @ 30fps)
  2. 滑动窗口维护 context_size+1 帧历史
  3. DDIM-5 步快速推理（Day 2 验证的最佳配置）
  4. 轨迹叠加实时显示
  5. 按键控制: 'g' 抓取目标帧, 'q' 退出, 's' 截图
  6. 完整录制 + 推理 FPS 统计

用法:
  cd /home/yifei/codespace/visualnav-transformer
  python scripts/realtime_inference.py [--camera 0] [--ddim-steps 5]
"""

import os
import sys
import time
import argparse
from datetime import datetime
from collections import deque

import cv2
import numpy as np
import torch
import matplotlib
matplotlib.use("Agg")  # 无头渲染，避免 GUI 冲突
import matplotlib.pyplot as plt
from torchvision import transforms
from PIL import Image as PILImage

# ── Run Tag ───────────────────────────────────────────────
RUN_TAG = datetime.now().strftime("%Y%m%d_%H%M%S")

# ── Project Paths ─────────────────────────────────────────
PROJECT_ROOT = "/home/yifei/codespace/visualnav-transformer"
sys.path.insert(0, os.path.join(PROJECT_ROOT, "train"))
sys.path.insert(0, os.path.join(PROJECT_ROOT, "deployment/src"))
sys.path.insert(0, os.path.join(PROJECT_ROOT, "scripts"))

from offline_inference import (
    build_model, ENCODING_SIZE, LEN_TRAJ_PRED, IMAGE_SIZE,
    CONTEXT_SIZE, NUM_DIFFUSION_ITERS,
)
from diffusers.schedulers.scheduling_ddim import DDIMScheduler
from diffusers.schedulers.scheduling_ddpm import DDPMScheduler


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#  图像预处理
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
_TRANSFORM = transforms.Compose([
    transforms.ToTensor(),
    transforms.Normalize(mean=[0.485, 0.456, 0.406],
                         std=[0.229, 0.224, 0.225]),
])


def frame_to_tensor(frame_bgr: np.ndarray) -> torch.Tensor:
    """OpenCV BGR 帧 -> NoMaD 输入张量 [3, 96, 96]"""
    rgb = cv2.cvtColor(frame_bgr, cv2.COLOR_BGR2RGB)
    pil = PILImage.fromarray(rgb).resize(IMAGE_SIZE)
    return _TRANSFORM(pil)


def build_obs_tensor(frame_buffer: deque) -> torch.Tensor:
    """从滑动窗口构建 [1, (C+1)*3, 96, 96] 观测张量"""
    frames = list(frame_buffer)
    # 帧数不足时用最早的帧填充
    while len(frames) < CONTEXT_SIZE + 1:
        frames.insert(0, frames[0].clone())
    frames = frames[-(CONTEXT_SIZE + 1):]
    return torch.cat(frames, dim=0).unsqueeze(0)


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#  DDIM 推理
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
def realtime_diffusion(model, obs_cond, device, num_steps=5, num_samples=8):
    """DDIM 快速推理，返回 [num_samples, 8, 2] 轨迹"""
    scheduler = DDIMScheduler(
        num_train_timesteps=NUM_DIFFUSION_ITERS,
        beta_schedule="squaredcos_cap_v2",
        clip_sample=True,
        prediction_type="epsilon",
    )
    cond = obs_cond.repeat(num_samples, 1)
    naction = torch.randn((num_samples, LEN_TRAJ_PRED, 2), device=device)
    scheduler.set_timesteps(num_steps)
    with torch.no_grad():
        for k in scheduler.timesteps:
            noise_pred = model("noise_pred_net", sample=naction,
                               timestep=k, global_cond=cond)
            naction = scheduler.step(model_output=noise_pred,
                                     timestep=k, sample=naction).prev_sample
    return naction


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#  轨迹绘制
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
def draw_trajectories(frame: np.ndarray, trajectories: torch.Tensor,
                      fps: float, mode: str) -> np.ndarray:
    """在帧上叠加路径点轨迹和状态信息"""
    h, w = frame.shape[:2]
    overlay = frame.copy()
    trajs = trajectories.cpu().numpy()
    center_x, center_y = w // 2, h - 30  # 底部中心作为机器人位置

    # 坐标映射: NoMaD 路径点 (x=前进, y=左) -> 像素 (u=右, v=上)
    scale = min(w, h) / 2.0  # 缩放因子

    colors = [
        (52, 152, 219), (46, 204, 113), (155, 89, 182),
        (241, 196, 15), (230, 126, 34), (231, 76, 60),
        (26, 188, 156), (149, 165, 166),
    ]

    # 绘制平均轨迹（粗线）
    mean_traj = trajs.mean(axis=0)
    pts_mean = []
    for wp in mean_traj:
        px = int(center_x + wp[1] * scale)  # y->right
        py = int(center_y - wp[0] * scale)  # x->up
        pts_mean.append((px, py))
    for i in range(len(pts_mean) - 1):
        cv2.line(overlay, pts_mean[i], pts_mean[i+1], (0, 255, 0), 3)
    for pt in pts_mean:
        cv2.circle(overlay, pt, 5, (0, 255, 0), -1)

    # 绘制单个采样轨迹（细线，半透明）
    for s in range(min(trajs.shape[0], 4)):
        pts = []
        for wp in trajs[s]:
            px = int(center_x + wp[1] * scale)
            py = int(center_y - wp[0] * scale)
            pts.append((px, py))
        color = colors[s % len(colors)]
        for i in range(len(pts) - 1):
            cv2.line(overlay, pts[i], pts[i+1], color, 1)

    # 机器人位置标记
    cv2.circle(overlay, (center_x, center_y), 8, (0, 0, 255), -1)
    cv2.putText(overlay, "R", (center_x - 5, center_y + 4),
                cv2.FONT_HERSHEY_SIMPLEX, 0.4, (255, 255, 255), 1)

    # 屏幕信息叠加
    cv2.putText(overlay, f"Mode: {mode}", (10, 25),
                cv2.FONT_HERSHEY_SIMPLEX, 0.6, (0, 255, 0), 2)
    cv2.putText(overlay, f"FPS: {fps:.1f}", (10, 50),
                cv2.FONT_HERSHEY_SIMPLEX, 0.6, (0, 255, 0), 2)
    cv2.putText(overlay, f"DDIM Steps: 5", (10, 75),
                cv2.FONT_HERSHEY_SIMPLEX, 0.6, (0, 255, 0), 2)
    cv2.putText(overlay, "[G]oal  [S]creenshot  [Q]uit", (10, h - 10),
                cv2.FONT_HERSHEY_SIMPLEX, 0.5, (200, 200, 200), 1)

    return cv2.addWeighted(overlay, 0.8, frame, 0.2, 0)


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#  主循环
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
def main():
    parser = argparse.ArgumentParser(description="NoMaD Realtime Camera Inference")
    parser.add_argument("--camera", type=int, default=0,
                        help="Camera device index (default /dev/video0)")
    parser.add_argument("--ddim-steps", type=int, default=5,
                        help="DDIM denoising steps (default 5)")
    parser.add_argument("--width", type=int, default=640)
    parser.add_argument("--height", type=int, default=480)
    parser.add_argument("--no-display", action="store_true",
                        help="Headless mode (for SSH without GUI)")
    args = parser.parse_args()

    device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
    print(f"Device: {device}")

    # 1. 加载模型
    model = build_model(device)

    # 2. 打开摄像头
    cap = cv2.VideoCapture(args.camera)
    if not cap.isOpened():
        print(f"Error: Cannot open camera /dev/video{args.camera}")
        print("Hint: Check camera connection, or try --camera 1")
        return
    cap.set(cv2.CAP_PROP_FRAME_WIDTH, args.width)
    cap.set(cv2.CAP_PROP_FRAME_HEIGHT, args.height)
    actual_w = int(cap.get(cv2.CAP_PROP_FRAME_WIDTH))
    actual_h = int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT))
    print(f"Camera opened: {actual_w}x{actual_h}")

    # 3. 输出目录
    out_dir = os.path.join(PROJECT_ROOT, f"results/realtime/{RUN_TAG}_realtime")
    os.makedirs(out_dir, exist_ok=True)

    # 4. 视频录制器
    fourcc = cv2.VideoWriter_fourcc(*"mp4v")
    video_path = os.path.join(out_dir, "recording.mp4")
    writer = cv2.VideoWriter(video_path, fourcc, 15.0,
                             (actual_w, actual_h))

    # 5. 初始化
    frame_buffer = deque(maxlen=CONTEXT_SIZE + 1)
    goal_frame = None       # 目标帧（按 G 设置）
    goal_tensor = None
    mode = "Explore (mask=1)"
    screenshot_count = 0
    fps_list = []

    print("\n" + "=" * 50)
    print("Realtime inference started!")
    print("  [G] Grab current frame as goal (switch to navigation mode)")
    print("  [S] Save screenshot")
    print("  [Q] Quit")
    print("=" * 50)

    try:
        while True:
            ret, frame = cap.read()
            if not ret:
                print("Camera read failed")
                break

            t0 = time.time()

            # 预处理当前帧
            tensor = frame_to_tensor(frame)
            frame_buffer.append(tensor)

            # 等待至少 context_size+1 帧
            if len(frame_buffer) < CONTEXT_SIZE + 1:
                cv2.putText(frame, f"Buffering... {len(frame_buffer)}/{CONTEXT_SIZE+1}",
                            (10, 30), cv2.FONT_HERSHEY_SIMPLEX, 0.7, (0, 255, 255), 2)
                if not args.no_display:
                    cv2.imshow("NoMaD Realtime", frame)
                    cv2.waitKey(1)
                continue

            # 构建输入
            obs_tensor = build_obs_tensor(frame_buffer).to(device)

            if goal_tensor is not None:
                # 导航模式
                goal_input = goal_tensor.to(device)
                mask = torch.zeros(1).long().to(device)
                mode = "Navigate (mask=0)"
            else:
                # 探索模式 — 用当前帧作为虚拟目标
                goal_input = tensor.unsqueeze(0).to(device)
                mask = torch.ones(1).long().to(device)
                mode = "Explore (mask=1)"

            # 视觉编码
            with torch.no_grad():
                obs_cond = model("vision_encoder",
                                 obs_img=obs_tensor,
                                 goal_img=goal_input,
                                 input_goal_mask=mask)

            # DDIM 推理
            trajectories = realtime_diffusion(
                model, obs_cond, device,
                num_steps=args.ddim_steps,
            )

            elapsed = time.time() - t0
            fps = 1.0 / elapsed if elapsed > 0 else 0
            fps_list.append(fps)

            # 在帧上叠加轨迹
            display_frame = draw_trajectories(frame, trajectories, fps, mode)

            # 显示目标帧缩略图
            if goal_frame is not None:
                thumb = cv2.resize(goal_frame, (120, 90))
                display_frame[5:95, actual_w-125:actual_w-5] = thumb
                cv2.putText(display_frame, "GOAL", (actual_w-120, 20),
                            cv2.FONT_HERSHEY_SIMPLEX, 0.5, (0, 255, 0), 1)

            # 写入录制
            writer.write(display_frame)

            # 显示
            if not args.no_display:
                cv2.imshow("NoMaD Realtime", display_frame)

            # 键盘事件
            key = cv2.waitKey(1) & 0xFF
            if key == ord("q"):
                print("\nExiting realtime inference")
                break
            elif key == ord("g"):
                goal_frame = frame.copy()
                goal_tensor = tensor.unsqueeze(0)
                print(f"  -> Goal frame set (switching to navigation mode)")
                # 保存目标帧
                cv2.imwrite(os.path.join(out_dir, "goal_frame.jpg"), goal_frame)
            elif key == ord("s"):
                screenshot_count += 1
                ss_path = os.path.join(out_dir,
                    f"screenshot_{screenshot_count:03d}.jpg")
                cv2.imwrite(ss_path, display_frame)
                print(f"  -> Screenshot saved: {ss_path}")

    finally:
        cap.release()
        writer.release()
        if not args.no_display:
            cv2.destroyAllWindows()

    # ── Save Statistics Summary ──────────────────────
    if fps_list:
        summary_path = os.path.join(out_dir, "summary.txt")
        avg_fps = np.mean(fps_list)
        with open(summary_path, "w") as f:
            f.write(f"Run Time: {RUN_TAG}\n")
            f.write(f"Script: realtime_inference.py\n")
            f.write(f"Camera: /dev/video{args.camera}\n")
            f.write(f"Resolution: {actual_w}x{actual_h}\n")
            f.write(f"DDIM Steps: {args.ddim_steps}\n")
            f.write(f"Total Frames: {len(fps_list)}\n")
            f.write(f"Avg Inference FPS: {avg_fps:.1f}\n")
            f.write(f"Min FPS: {min(fps_list):.1f}\n")
            f.write(f"Max FPS: {max(fps_list):.1f}\n")
            f.write(f"Recording: {video_path}\n")
            f.write(f"Screenshots: {screenshot_count}\n")
            f.write(f"\nLegged Deployment Evaluation:\n")
            if avg_fps >= 15:
                f.write(f"  ✅ Avg {avg_fps:.1f} Hz meets legged navigation requirement (≥15 Hz)\n")
            else:
                f.write(f"  ⚠️ Avg {avg_fps:.1f} Hz below legged threshold 15 Hz, "
                        f"reduce DDIM steps or lower sample count\n")

        print(f"\n{'='*50}")
        print(f"Realtime inference statistics:")
        print(f"  Avg FPS: {avg_fps:.1f}")
        print(f"  Recording saved: {video_path}")
        print(f"  Summary saved: {summary_path}")
        print(f"{'='*50}")

    # ── Generate FPS Trend Chart ──────────────────────
    if len(fps_list) > 10:
        fig, ax = plt.subplots(1, 1, figsize=(10, 4))
        ax.plot(fps_list, color="#3498db", alpha=0.6, linewidth=0.5, label="instant FPS")
        # 移动平均
        window = min(30, len(fps_list) // 3)
        if window > 1:
            ma = np.convolve(fps_list, np.ones(window)/window, mode="valid")
            ax.plot(range(window-1, len(fps_list)), ma,
                    color="#e74c3c", linewidth=2, label=f"MA-{window}")
        ax.axhline(y=15, color="orange", linestyle="--", label="Legged threshold 15Hz")
        ax.axhline(y=10, color="red", linestyle=":", alpha=0.5, label="Wheeled baseline 10Hz")
        ax.set_xlabel("Frame Index")
        ax.set_ylabel("FPS")
        ax.set_title("Realtime Inference FPS Trend")
        ax.legend(fontsize=9)
        ax.grid(True, alpha=0.3)
        plt.tight_layout()
        plt.savefig(os.path.join(out_dir, "fps_trend.png"), dpi=150)
        plt.close()
        print(f"  FPS trend: {out_dir}/fps_trend.png")


if __name__ == "__main__":
    main()
```

#### 使用说明

> **📁 文件放置**：将上述代码保存到远程训练机的  
> `/home/yifei/codespace/visualnav-transformer/scripts/realtime_inference.py`

**前置条件**：

1. 训练机 **直接连接** USB 摄像头（或通过 SSH X11 转发）
2. Day 1 已安装依赖（`opencv-python` 在 `requirements.txt` 中）
3. 权重已就位（`deployment/model_weights/nomad/nomad.pth`）

**运行方式**：

```bash
cd /home/yifei/codespace/visualnav-transformer

# 方式一：有显示器（直接或 X11 转发）
python scripts/realtime_inference.py

# 方式二：无头模式（SSH 无 GUI，仅录像不显示）
python scripts/realtime_inference.py --no-display

# 方式三：自定义参数
python scripts/realtime_inference.py --camera 1 --ddim-steps 3 --width 320 --height 240
```

**操作流程**：

1. 启动后默认**探索模式**（`mask=1`），轨迹应分散朝各方向
2. 手持摄像头 / 放在桌面上，对准走廊或开放空间
3. 按 **`G`** 键抓取当前帧作为目标 → 自动切换到**导航模式**
4. 观察轨迹是否朝目标方向聚集
5. 按 **`S`** 保存当前帧截图（含轨迹叠加）
6. 按 **`Q`** 退出，自动保存录像和统计

**输出文件**：

```
results/realtime/YYYYMMDD_HHMMSS_realtime/
├── recording.mp4          ← 全程录像（含轨迹叠加）
├── goal_frame.jpg         ← 设定的目标帧
├── screenshot_001.jpg     ← 手动截图
├── summary.txt            ← 推理频率统计
└── fps_trend.png          ← FPS 趋势图
```

> 💡 **科研价值**：此实验的录像和 FPS 数据可直接用于论文中
> "系统实时性验证"章节，证明 DDIM-5 步在真实视频流下的推理频率。

---

### 2.5 Day 2 验收清单

- [ ] `DDIMScheduler` / `DDPMScheduler` 可正常导入
- [ ] 生成了 6 种配置的对比图表（含逐配置独立轨迹图 + Hz 阈值图）
- [ ] 确认 DDIM-5 步的轨迹质量与 DDPM-10 步接近
- [ ] 记录了加速比（预期 2–3×）
- [ ] 确定最佳 DDIM 步数
- [ ] **分析了最佳配置能否将决策频率提升到 ≥15 Hz（足式部署门槛）**
- [ ] 实时推理脚本可正常打开摄像头并显示画面
- [ ] 按 `G` 可设定目标帧并切换到导航模式
- [ ] 录像和 FPS 统计已保存到 `results/realtime/` 目录
- [ ] `summary.txt` 中平均 FPS 数据可用于论文引用

---

## Day 3（周三）：CFG 目标引导 + 步态抖动鲁棒性实验

### 3.1 研究动机：CFG 对足式部署的价值

> **科研视角**：足式机器人的步态抖动会导致视觉输入器噪，可能使 waypoint 偏移目标方向。
> CFG 通过增强目标引导信号，有望**补偿步态干扰引起的轨迹偏差**。

### 3.2 理解 CFG 原理（≈ 30 min）

**核心思想**：NoMaD 训练时以 50% 概率遮蔽目标图像（`goal_mask_prob: 0.5`），
因此模型同时学会了：

| 模式 | mask | 含义 |
|------|------|------|
| 有条件生成 | `mask = 0` | 看到目标 → 导航模式 |
| 无条件生成 | `mask = 1` | 看不到目标 → 探索模式 |

**CFG 公式**：

$$\epsilon_\text{final} = (1 + w)\,\epsilon_\text{cond} - w\,\epsilon_\text{uncond}$$

令 $s = 1 + w$（$s$ 为经典 Classifier-Free Guidance 的引导缩放系数），等价形式为：

$$\epsilon_\text{final} = s\,\epsilon_\text{cond} + (1 - s)\,\epsilon_\text{uncond}$$

> ⚠️ **关键辨析**：`w = 0` **不是**"无引导"，而是**标准条件生成**——模型正常使用目标图像导航，
> 只是没有额外放大引导信号。真正的"无引导"（纯无条件生成 / 纯探索模式）对应 **`w = −1`**（即 $s = 0$）。

| `w` 值 | 等价 $s = 1+w$ | 公式退化为 | 物理含义 |
|--------|---------------|------------|----------|
| `−1` | $s = 0$ | $\epsilon_\text{uncond}$ | **纯探索模式** — 完全忽略目标图像 |
| `0` | $s = 1$ | $\epsilon_\text{cond}$ | **标准条件生成** — 正常目标导航，无额外增强 |
| `0 < w ≤ 2` | $1 < s ≤ 3$ | 加权组合 | **引导增强** — 放大目标方向信号，轨迹更集中 |
| `w > 2` | $s > 3$ | 加权组合 | **过度引导** — 可能过于激进，轨迹质量下降 |

> **关键价值**：NoMaD 原始代码 **完全没有使用 CFG**，但 Goal Masking 训练方式
> 天然满足 CFG 的前提条件。`train_utils.py` 中的 `model_output()` 函数已经分别做了
> `mask=0` 和 `mask=1` 的推理（返回 `gc_actions` 和 `uc_actions`），
> 证明基础设施已就绪。

### 3.2 编写 CFG 实验脚本

创建 `scripts/cfg_experiment.py`：

```python
#!/usr/bin/env python3
"""
Day 3: 无分类器引导 (CFG) 实验
研究不同引导强度 (w) 对轨迹目标导向性和多样性的影响。

CFG 公式:  ε_final = (1 + w) · ε_cond − w · ε_uncond
  - w = −1  → 纯无条件生成（完全忽略目标 — 探索模式）
  - w = 0   → 标准条件生成（使用目标导航，无额外增强）
  - w > 0   → 引导增强（放大目标方向信号）
"""

import os
import sys
import time
from datetime import datetime

import numpy as np
import torch
import matplotlib.pyplot as plt
from matplotlib.gridspec import GridSpec

RUN_TAG = datetime.now().strftime("%Y%m%d_%H%M%S")

PROJECT_ROOT = "/home/yifei/codespace/visualnav-transformer"
sys.path.insert(0, os.path.join(PROJECT_ROOT, "train"))
sys.path.insert(0, os.path.join(PROJECT_ROOT, "deployment/src"))
sys.path.insert(0, os.path.join(PROJECT_ROOT, "scripts"))

from diffusers.schedulers.scheduling_ddpm import DDPMScheduler

from offline_inference import (
    build_model, prepare_observation, prepare_goal,
    DATASET_ROOT, CONTEXT_SIZE, IMAGE_SIZE, LEN_TRAJ_PRED,
)


def cfg_inference(
    model, obs_img, goal_img, device,
    w: float = 0.0,
    num_samples: int = 8,
    num_steps: int = 10,
):
    """
    带无分类器引导的扩散推理。

    每个去噪步骤需要 **两次** 前向传播:
      1. mask=0 → ε_cond    (导航方向)
      2. mask=1 → ε_uncond  (探索方向)
    然后: ε_final = (1+w)·ε_cond − w·ε_uncond
    """
    scheduler = DDPMScheduler(
        num_train_timesteps=num_steps,
        beta_schedule="squaredcos_cap_v2",
        clip_sample=True,
        prediction_type="epsilon",
    )

    # ── 视觉编码（仅各执行一次，不在去噪循环内重复）──
    mask_nav     = torch.zeros(1).long().to(device)
    mask_explore = torch.ones(1).long().to(device)

    with torch.no_grad():
        cond_enc   = model("vision_encoder", obs_img=obs_img,
                           goal_img=goal_img, input_goal_mask=mask_nav)
        uncond_enc = model("vision_encoder", obs_img=obs_img,
                           goal_img=goal_img, input_goal_mask=mask_explore)

    cond_rep   = cond_enc.repeat(num_samples, 1)
    uncond_rep = uncond_enc.repeat(num_samples, 1)

    naction = torch.randn((num_samples, LEN_TRAJ_PRED, 2), device=device)
    scheduler.set_timesteps(num_steps)

    with torch.no_grad():
        for k in scheduler.timesteps:
            eps_cond = model("noise_pred_net", sample=naction,
                             timestep=k, global_cond=cond_rep)

            if w != 0.0:
                eps_uncond = model("noise_pred_net", sample=naction,
                                   timestep=k, global_cond=uncond_rep)
                # CFG 核心公式: ε_final = (1+w)·ε_cond − w·ε_uncond
                # w=-1 → 纯探索(ε_uncond); w>0 → 增强引导
                eps_final = (1.0 + w) * eps_cond - w * eps_uncond
            else:
                # w=0: 标准条件生成，跳过无条件前向传播以节省计算
                eps_final = eps_cond

            naction = scheduler.step(
                model_output=eps_final, timestep=k, sample=naction,
            ).prev_sample

    return naction


def main():
    device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
    model  = build_model(device)

    # 准备测试数据
    trajs     = sorted(os.listdir(DATASET_ROOT))
    traj_path = os.path.join(DATASET_ROOT, trajs[0])
    obs_time  = CONTEXT_SIZE + 2
    goal_time = obs_time + 15

    obs_img  = prepare_observation(traj_path, obs_time).to(device)
    goal_img = prepare_goal(traj_path, goal_time).to(device)

    # ── 实验 ──────────────────────────────────────────────
    w_values = [-1.0, 0.0, 0.5, 1.0, 2.0, 4.0]
    results  = {}

    print("=" * 70)
    print("Classifier-Free Guidance Experiment")
    print("=" * 70)

    for w in w_values:
        print(f"\n--- w = {w} ---")
        t0 = time.time()
        out = cfg_inference(model, obs_img, goal_img, device, w=w)
        elapsed = time.time() - t0

        trajs_np  = out.cpu().numpy()
        mean_traj = trajs_np.mean(axis=0)
        diversity = trajs_np.std(axis=0).mean()
        mean_dist = np.sqrt((mean_traj ** 2).sum(axis=1)).mean()

        results[w] = dict(
            trajs=trajs_np, mean_traj=mean_traj,
            diversity=diversity, mean_dist=mean_dist, time=elapsed,
        )
        print(f"  Time: {elapsed*1000:.1f} ms | "
              f"Diversity: {diversity:.4f} | Mean Displacement: {mean_dist:.4f}")

    # ── Visualization 1: Trajectory distribution for each w ────────
    out_dir = os.path.join(PROJECT_ROOT, "results/day3")
    os.makedirs(out_dir, exist_ok=True)

    fig = plt.figure(figsize=(20, 12))
    gs  = GridSpec(2, 3, figure=fig)

    for idx, w in enumerate(w_values):
        ax   = fig.add_subplot(gs[idx // 3, idx % 3])
        data = results[w]
        for i in range(data["trajs"].shape[0]):
            t = data["trajs"][i]
            ax.plot(t[:, 1], t[:, 0], "o-", alpha=0.4, markersize=2)
        mt = data["mean_traj"]
        ax.plot(mt[:, 1], mt[:, 0], "k-", linewidth=2.5, label="mean")
        ax.plot(0, 0, "r*", markersize=12)
        ax.set_title(f"w = {w} | div = {data['diversity']:.3f}")
        ax.set_aspect("equal")
        ax.grid(True, alpha=0.3)
        ax.legend(fontsize=8)

    plt.suptitle("Classifier-Free Guidance: Trajectory Comparison for Different w", fontsize=16, y=1.02)
    plt.tight_layout()
    plt.savefig(os.path.join(out_dir, "cfg_comparison.png"),
                dpi=150, bbox_inches="tight")
    plt.close()

    # ── 可视化 2: 趋势图 ─────────────────────────────────
    fig, axes = plt.subplots(1, 3, figsize=(15, 4))
    ws = list(results.keys())

    axes[0].plot(ws, [results[w]["diversity"]  for w in ws], "bo-")
    axes[0].set_xlabel("w")
    axes[0].set_ylabel("Diversity (std)")
    axes[0].set_title("Diversity vs w")

    axes[1].plot(ws, [results[w]["mean_dist"]  for w in ws], "ro-")
    axes[1].set_xlabel("w")
    axes[1].set_ylabel("Mean Displacement")
    axes[1].set_title("Goal-Directedness vs w")

    axes[2].plot(ws, [results[w]["time"] * 1000 for w in ws], "go-")
    axes[2].set_xlabel("w")
    axes[2].set_ylabel("Inference Time (ms)")
    axes[2].set_title("Inference Time vs w")

    plt.tight_layout()
    plt.savefig(os.path.join(out_dir, "cfg_trends.png"), dpi=150)
    plt.close()

    # ── 可视化 3: 单独保存每个 w 值 ─────────
    for w_val in w_values:
        fig_w, ax_w = plt.subplots(1, 1, figsize=(6, 6))
        data = results[w_val]
        for i in range(data["trajs"].shape[0]):
            t = data["trajs"][i]
            ax_w.plot(t[:, 1], t[:, 0], "o-", alpha=0.4, markersize=2)
        mt = data["mean_traj"]
        ax_w.plot(mt[:, 1], mt[:, 0], "k-", linewidth=2.5, label="mean")
        ax_w.plot(0, 0, "r*", markersize=12)
        ax_w.set_title(f"CFG w={w_val} | div={data['diversity']:.3f}")
        ax_w.set_aspect("equal"); ax_w.grid(True, alpha=0.3)
        ax_w.legend(fontsize=8)
        plt.tight_layout()
        plt.savefig(os.path.join(out_dir, f"cfg_w{w_val:.1f}.png"), dpi=150)
        plt.close()

    # ── 保存结果到 txt ─────────────────────────
    txt_path = os.path.join(out_dir, "results.txt")
    with open(txt_path, "w") as f:
        f.write(f"Run Time: {RUN_TAG}\nScript: cfg_experiment.py\n\n")
        f.write(f"{'w':<8} {'Diversity':<12} {'Mean Disp.':<12} {'Time(ms)':<12}\n")
        f.write("-" * 44 + "\n")
        for w_val in ws:
            r = results[w_val]
            f.write(f"{w_val:<8.1f} {r['diversity']:<12.4f} "
                    f"{r['mean_dist']:<12.4f} {r['time']*1000:<12.1f}\n")
    print(f"\nResults saved: {txt_path}")

    # ── Summary ──────────────────────────────────────────────
    print("\n" + "=" * 70)
    print(f"{'w':<8} {'Diversity':<12} {'Mean Disp.':<12} {'Time(ms)':<12}")
    print("-" * 70)
    for w in ws:
        r = results[w]
        print(f"{w:<8.1f} {r['diversity']:<12.4f} "
              f"{r['mean_dist']:<12.4f} {r['time']*1000:<12.1f}")
    print("=" * 70)
    print("\n预期观察:")
    print("  w=-1    : 纯探索模式，最高多样性，无目标导向")
    print("  w=0     : 标准条件生成（目标导航基线）")
    print("  w=1~2   : 引导增强，多样性降低，轨迹更集中朝目标（最佳区间）")
    print("  w=4     : 过度引导，可能过于激进，轨迹质量下降")


if __name__ == "__main__":
    main()
```

> **📁 文件放置**：将上述代码保存到远程训练机的  
> `/home/yifei/codespace/visualnav-transformer/scripts/cfg_experiment.py`  
> **依赖**：需要 Day 1 的 `offline_inference.py` 已放置在同目录  
> **✅ 运行方式**：
>
> ```bash
> cd /home/yifei/codespace/visualnav-transformer
> python scripts/cfg_experiment.py
> ```

---

### 3.3 补充实验：DDIM + CFG 联合

下午将 Day 2 和 Day 3 成果结合：

| 配置 | 预期 |
|------|------|
| DDIM-5 步 + w=1.0 vs DDPM-10 步 | 速度接近 DDIM-5（比基线快 ≈ 2×），质量因 CFG 提升 |

> 实现方式：在 `cfg_inference()` 中把 `DDPMScheduler` 换成 `DDIMScheduler`，
> 并将 `num_steps=5`。

---

### 3.5 步态抖动鲁棒性实验（下午 ≈ 2 h）

> **科研空白 G4**：足式机器人运动时相机产生周期性抖动（俯仰±3°、垂向平移±5px）。
> 这是轮式平台不存在的问题，尚未有人量化其对扩散导航策略的影响。

**实验设计**：在 NoMaD 推理输入上人为添加四足步态式的周期性图像变换，
对比有/无抖动条件下的 waypoint 预测一致性。所有条件（含 `no_shake`）走**同一条处理
流水线**（PIL 加载 → 抖动 → 归一化），`no_shake`（抖动参数全零）天然等价于标准推理，
直接作为基线（`cos_sim = 1.0`），避免因独立基线路径引入的处理差异。

**创建** `scripts/gait_shake_robustness.py`：

```python
#!/usr/bin/env python3
"""
Day 3: 步态抖动鲁棒性实验

研究问题: 四足机器人运动时，相机会跟随机体运动产生周期性抖动。
这种抖动在轮式平台上不存在，是足式部署的独特挑战。

实验方法:
  1. 对输入图像施加模拟步态周期性变换（旋转±α、平移±d）
  2. 对比干净与抖动图像下的路径点预测差异
  3. 测试 CFG 能否减轻抖动干扰（结合 Day 3 上午结果）
"""

import os
import sys
import numpy as np
import torch
import matplotlib.pyplot as plt
from datetime import datetime
from torchvision import transforms as T
from PIL import Image as PILImage

RUN_TAG = datetime.now().strftime("%Y%m%d_%H%M%S")

PROJECT_ROOT = "/home/yifei/codespace/visualnav-transformer"
sys.path.insert(0, os.path.join(PROJECT_ROOT, "train"))
sys.path.insert(0, os.path.join(PROJECT_ROOT, "deployment/src"))
sys.path.insert(0, os.path.join(PROJECT_ROOT, "scripts"))

from offline_inference import (
    build_model, prepare_goal,
    diffusion_inference, _find_image,
    DATASET_ROOT, CONTEXT_SIZE, LEN_TRAJ_PRED, IMAGE_SIZE,
)

# ── Gait Shake Simulation ─────────────────────────────
# DeepRobotics Lite3 typical gait parameters:
#   Pitch oscillation: ±2–3° (gait frequency ≈2.5 Hz walk / 3.5 Hz trot)
#   Vertical oscillation: ±5–10 mm
#   Roll oscillation: ±1–2°
GAIT_CONFIGS = {
    "no_shake": {"angle": 0.0, "shift_x": 0, "shift_y": 0},
    "mild_shake (walk)": {"angle": 1.5, "shift_x": 2, "shift_y": 3},
    "moderate_shake (fast_walk)": {"angle": 3.0, "shift_x": 4, "shift_y": 5},
    "severe_shake (trot)": {"angle": 5.0, "shift_x": 6, "shift_y": 8},
}


def apply_gait_shake(
    img_pil: PILImage.Image,
    angle_deg: float,
    shift_x: int, shift_y: int,
    phase: float = 0.0,
) -> PILImage.Image:
    """
    模拟四足步态引起的相机周期性抖动。
    在 PIL 空间操作，避免归一化后的张量被 ToPILImage 截断破坏。

    参数:
        img_pil: 原始 PIL 图像（未归一化）
        angle_deg: 最大旋转角度（俯仰）
        shift_x, shift_y: 最大平移像素
        phase: 步态相位 (0–2π)
    返回:
        抖动后的 PIL 图像（未归一化）
    """
    if angle_deg == 0 and shift_x == 0 and shift_y == 0:
        return img_pil                          # 无抖动时直接返回，零开销

    actual_angle = angle_deg * np.sin(phase)
    actual_sx = int(shift_x * np.sin(phase))
    actual_sy = int(shift_y * np.cos(phase * 0.5))

    img_pil = img_pil.rotate(actual_angle, fillcolor=(128, 128, 128))

    arr = np.array(img_pil)
    arr = np.roll(arr, actual_sx, axis=1)
    arr = np.roll(arr, actual_sy, axis=0)
    return PILImage.fromarray(arr)


def apply_shake_to_observation(
    traj_path: str, t: int,
    angle_deg: float, shift_x: int, shift_y: int,
) -> torch.Tensor:
    """
    为 context_size+1 观测帧施加不同相位的步态抖动。

    修正：抖动在 PIL 空间施加（归一化之前），ImageNet 归一化仅在最后
    执行一次。这保证 no_shake 条件与 prepare_observation() 完全等价，
    天然成为基线。
    """
    from offline_inference import _TRANSFORM

    frames = []
    indices = list(range(max(0, t - CONTEXT_SIZE), t + 1))
    while len(indices) < CONTEXT_SIZE + 1:
        indices.insert(0, indices[0])
    indices = indices[-(CONTEXT_SIZE + 1):]

    for i, idx in enumerate(indices):
        # 1) 加载原始 PIL 图像（未归一化）
        img_pil = PILImage.open(_find_image(traj_path, idx)).convert("RGB")
        img_pil = img_pil.resize(IMAGE_SIZE)

        # 2) 在 PIL 空间施加步态抖动
        phase = 2 * np.pi * i / (CONTEXT_SIZE + 1)  # 模拟步态周期
        img_pil = apply_gait_shake(img_pil, angle_deg, shift_x, shift_y, phase)

        # 3) 统一归一化（ToTensor + ImageNet Normalize，仅此一次）
        img = _TRANSFORM(img_pil)
        frames.append(img)

    return torch.cat(frames, dim=0).unsqueeze(0)


def cosine_similarity(a: np.ndarray, b: np.ndarray) -> float:
    """计算两条轨迹之间的余弦相似度"""
    a_flat = a.flatten()
    b_flat = b.flatten()
    return float(np.dot(a_flat, b_flat) / (
        np.linalg.norm(a_flat) * np.linalg.norm(b_flat) + 1e-8
    ))


def main():
    device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
    model  = build_model(device)

    trajs     = sorted(os.listdir(DATASET_ROOT))
    traj_path = os.path.join(DATASET_ROOT, trajs[0])
    obs_time  = CONTEXT_SIZE + 2
    goal_time = obs_time + 15

    goal_img = prepare_goal(traj_path, goal_time).to(device)
    mask = torch.zeros(1).long().to(device)

    # ── 所有条件（含 no_shake）走同一条处理流水线 ────
    # no_shake (angle=0, shift=0) 的处理路径与 prepare_observation() 完全等价，
    # 天然成为基线，无需单独计算 baseline。
    raw_results = {}
    print("=" * 70)
    print("Gait Shake Robustness Experiment")
    print("=" * 70)

    for name, cfg in GAIT_CONFIGS.items():
        print(f"\n--- {name} (angle=±{cfg['angle']}°, "
              f"shift=±{cfg['shift_x']}/{cfg['shift_y']}px) ---")

        obs = apply_shake_to_observation(
            traj_path, obs_time,
            cfg["angle"], cfg["shift_x"], cfg["shift_y"],
        ).to(device)

        with torch.no_grad():
            cond = model("vision_encoder", obs_img=obs,
                         goal_img=goal_img, input_goal_mask=mask)
        trajs_out = diffusion_inference(model, cond, device)
        raw_results[name] = trajs_out.mean(dim=0).cpu().numpy()

    # ── no_shake 即为基线 ──────────────────────────
    baseline = raw_results["no_shake"]

    # ── 计算各条件相对基线的指标 ──────────────────
    results = {}
    for name in GAIT_CONFIGS:
        mean_traj = raw_results[name]
        cos_sim = cosine_similarity(baseline, mean_traj)
        mse     = np.mean((baseline - mean_traj) ** 2)
        heading_err = np.abs(np.arctan2(mean_traj[0, 1], mean_traj[0, 0])
                            - np.arctan2(baseline[0, 1], baseline[0, 0]))

        results[name] = dict(cos_sim=cos_sim, mse=mse,
                             heading_err=np.degrees(heading_err),
                             mean_traj=mean_traj)
        print(f"  {name}: cos_sim={cos_sim:.4f} | MSE={mse:.6f} | "
              f"Heading Dev={np.degrees(heading_err):.2f}°")

    # ── 可视化 ───────────────────────────────────
    out_dir = os.path.join(PROJECT_ROOT, f"results/day3/{RUN_TAG}_gait_shake")
    os.makedirs(out_dir, exist_ok=True)
    names = list(results.keys())

    fig, axes = plt.subplots(1, 2, figsize=(14, 5))

    # 图表 1: 轨迹对比
    for name, r in results.items():
        t = r["mean_traj"]
        axes[0].plot(t[:, 1], t[:, 0], "o-", label=name, markersize=3)
    axes[0].plot(0, 0, "r*", markersize=15, label="robot")
    axes[0].set_title("Mean Trajectories Under Different Shake Intensities")
    axes[0].set_xlabel("y"); axes[0].set_ylabel("x")
    axes[0].legend(fontsize=8); axes[0].set_aspect("equal")
    axes[0].grid(True, alpha=0.3)

    # 图表 2: 鲁棒性指标（仅展示有抖动的条件，no_shake 恒为 1.0）
    shake_names = [n for n in names if n != "no_shake"]
    cos_vals = [results[n]["cos_sim"] for n in shake_names]
    colors = ["#3498db", "#e67e22", "#e74c3c"][:len(shake_names)]
    axes[1].bar(range(len(shake_names)), cos_vals, color=colors)
    axes[1].set_xticks(range(len(shake_names)))
    axes[1].set_xticklabels(shake_names, rotation=20, ha="right", fontsize=9)
    axes[1].axhline(y=0.95, color="green", linestyle="--", alpha=0.7, label="robust threshold")
    axes[1].set_ylabel("Cosine Similarity vs no_shake")
    axes[1].set_title("Visual Navigation Robustness to Gait Shake")
    axes[1].set_ylim(0.8, 1.01)
    axes[1].legend(fontsize=8)

    plt.tight_layout()
    plt.savefig(os.path.join(out_dir, "gait_shake_robustness.png"),
                dpi=150, bbox_inches="tight")
    plt.close()

    # ── 单独保存每个抖动级别的图表 ───────────────
    for name, r in results.items():
        fig_g, ax_g = plt.subplots(1, 1, figsize=(6, 6))
        t = r["mean_traj"]
        ax_g.plot(t[:, 1], t[:, 0], "o-", color="#e67e22", markersize=4, label=name)
        ax_g.plot(baseline[:, 1], baseline[:, 0], "--", color="#2ecc71",
                  linewidth=1.5, label="no_shake (baseline)")
        ax_g.plot(0, 0, "r*", markersize=15)
        ax_g.set_title(f"{name} | cos_sim={r['cos_sim']:.4f}")
        ax_g.set_aspect("equal"); ax_g.grid(True, alpha=0.3)
        ax_g.legend(fontsize=8); plt.tight_layout()
        safe = name.replace(" ", "_").replace("(", "").replace(")", "")
        plt.savefig(os.path.join(out_dir, f"shake_{safe}.png"), dpi=150)
        plt.close()

    # ── 保存结果到 txt ─────────────────────────
    txt_path = os.path.join(out_dir, "results.txt")
    with open(txt_path, "w") as f:
        f.write(f"Run Time: {RUN_TAG}\nScript: gait_shake_robustness.py\n\n")
        f.write(f"{'Shake Level':<20} {'cos_sim':<10} {'MSE':<12} {'Heading Dev.':<10}\n")
        f.write("-" * 52 + "\n")
        for n in names:
            r = results[n]
            f.write(f"{n:<20} {r['cos_sim']:<10.4f} {r['mse']:<12.6f} "
                    f"{r['heading_err']:<10.2f}\n")
        f.write("\nRobustness Assessment:\n")
        for n in names:
            r = results[n]
            if r["cos_sim"] > 0.95:
                f.write(f"  {n}: ✅ Navigation policy is robust to this shake level\n")
            else:
                f.write(f"  {n}: ❌ Needs additional compensation (CFG or image stabilization)\n")
    print(f"\nResults saved: {txt_path}")

    # ── 总结 ───────────────────────────────────
    print("\n" + "=" * 70)
    print(f"{'Shake Level':<20} {'cos_sim':<10} {'MSE':<12} {'Heading Dev.':<10}")
    print("-" * 70)
    for n in names:
        r = results[n]
        print(f"{n:<20} {r['cos_sim']:<10.4f} {r['mse']:<12.6f} "
              f"{r['heading_err']:<10.2f}°")
    print("=" * 70)
    print("\n预期观察:")
    print("  cos_sim > 0.95 -> 导航策略对该抖动级别具有鲁棒性")
    print("  cos_sim < 0.90 -> 足式部署需要额外补偿（如 CFG 增强）")


if __name__ == "__main__":
    main()
```

> **📁 文件放置**：将上述代码保存到远程训练机的  
> `/home/yifei/codespace/visualnav-transformer/scripts/gait_shake_robustness.py`  
> **依赖**：需要 Day 1 的 `offline_inference.py` 已放置在同目录  
> **✅ 运行方式**：
>
> ```bash
> cd /home/yifei/codespace/visualnav-transformer
> python scripts/gait_shake_robustness.py
> ```

**进阶：CFG 能否缓解抖动干扰？**

在上述实验基础上，对“中等抖动”场景追加 CFG (`w=1.0`) 推理：
如果 CFG 能将 `cos_sim` 从 <0.92 拉回到 >0.95，则证明
**CFG 对足式部署具有功能性价值（而不仅仅是实验技巧）**。

---

### 3.6 Day 3 验收清单

- [ ] CFG 推理逻辑正确（每步两次前向传播）
- [ ] 生成了 6 个 `w` 值的轨迹可视化
- [ ] 确认 `w = 1~2` 时轨迹更集中朝向目标
- [ ] 绘制了多样性 / 目标导向性 / 时间三个趋势图
- [ ] 测试了 DDIM + CFG 联合效果
- [ ] **步态抖动鲁棒性实验完成，生成了 4 个抖动等级对比图**
- [ ] **分析了 CFG 能否缓解抖动干扰（足式部署价值证据）**

---

## Day 4（周四）：视觉编码器升级（DINOv2）——增强跨形态泛化

### 4.1 研究动机：为什么 DINOv2 可能增强跨形态泛化？

> **科研空白 G5**：NoMaD 使用 EfficientNet-B0（ImageNet 监督预训练），
> 其特征对场景外观敏感、对视角变化可能不够鲁棒。
> DINOv2 的自监督特征更关注结构信息（轮廓、空间关系），
> 理论上更适合跨形态迁移（不同视高、不同视野）。
>
> **足式价值**：Lite3 的相机视高（≈0.32m）与 LoCoBot（≈0.4m）不同，
> 自监督特征对这种视角差异的泛化能力是一个开放问题。

### 4.2 安装 & 下载权重（≈ 10 min）

```bash
pip install "timm>=0.9.0"

# 验证 DINOv2-Small 通过 timm 加载（兼容 Python 3.8）
# ⚠️  不要使用 torch.hub.load('facebookresearch/dinov2', ...)：
#     上游仓库含 `float | None` 语法（PEP 604，Python 3.10+），
#     在 Python 3.8 环境下报 TypeError。timm 已内置兼容版本。
python -c "
import timm, torch
model = timm.create_model(
    'vit_small_patch14_dinov2.lvd142m',
    pretrained=True,
    num_classes=0,      # 返回 CLS token [B, 384]
)
model.eval()
x = torch.randn(1, 3, 98, 98)
with torch.no_grad():
    out = model(x)
print('DINOv2-Small loaded OK via timm')
print(f'输出形状: {out.shape}')        # → torch.Size([1, 384])
print(f'参数量: {sum(p.numel() for p in model.parameters()):,}')
"
```

| 编码器 | 参数量 | 输出维度 | 预训练方式 |
|--------|--------|----------|-----------|
| EfficientNet-B0（NoMaD 原版） | 5.3 M | 1280 → 256 | ImageNet 监督 |
| DINOv2-Small | 21 M | 384 → 256 | 自监督（更鲁棒） |

### 4.2 编写适配代码

创建 `scripts/nomad_vint_dinov2.py`：

```python
#!/usr/bin/env python3
"""
Day 4: 将 EfficientNet-B0 替换为 DINOv2-Small 作为 NoMaD 视觉编码器。

核心变更:
  - obs_encoder / goal_encoder 从 EfficientNet-B0 切换到 DINOv2-Small
  - 新增 384->256 投影层
  - DINOv2 要求输入尺寸为 14 的倍数: 96->98 (14x7)

注意: 使用 timm 加载 DINOv2（而非 torch.hub）。
  torch.hub.load('facebookresearch/dinov2', ...) 会拉取最新仓库代码，
  其中含有 'float | None' 联合类型语法（Python 3.10+），
  在 Python 3.8 上会导致 TypeError。timm 的 DINOv2 实现完全兼容。
"""

import sys
import os

import timm
import torch
import torch.nn as nn
import torch.nn.functional as F

PROJECT_ROOT = "/home/yifei/codespace/visualnav-transformer"
sys.path.insert(0, os.path.join(PROJECT_ROOT, "train"))


class DINOv2Encoder(nn.Module):
    """DINOv2-Small 图像编码器，输出固定维度特征向量。

    使用 timm 而非 torch.hub 以避免 Python 3.8 不兼容问题。
    timm 模型: vit_small_patch14_dinov2.lvd142m
    """

    def __init__(self, output_dim: int = 256, freeze_backbone: bool = True):
        super().__init__()
        # timm 加载 DINOv2-Small 预训练权重，不带分类头
        # num_classes=0      → forward() 返回 CLS token [B, 384]
        # dynamic_img_size=True → 禁用严格的 518×518 尺寸断言；
        #   位置编码在运行时插值以匹配输入尺寸
        #   （DINOv2 原生支持任何 patch_size=14 的倍数）
        self.backbone = timm.create_model(
            "vit_small_patch14_dinov2.lvd142m",
            pretrained=True,
            num_classes=0,
            dynamic_img_size=True,   # 允许 98×98 (= 14×7) 输入
        )
        self.feature_dim = 384              # DINOv2-Small CLS token 维度

        if freeze_backbone:
            for p in self.backbone.parameters():
                p.requires_grad = False
            print("[DINOv2] Backbone frozen — only projection layers trainable")

        self.projection = nn.Sequential(
            nn.Linear(self.feature_dim, output_dim),
            nn.GELU(),
            nn.Linear(output_dim, output_dim),
        )

    def forward(self, x: torch.Tensor) -> torch.Tensor:
        """x: [B, 3, H, W] → [B, output_dim]"""
        # DINOv2 patch_size=14，输入必须是 14 的倍数
        # NoMaD 默认 96x96 -> 调整为 98x98 (14x7)
        if x.shape[-1] != 98 or x.shape[-2] != 98:
            x = F.interpolate(x, size=(98, 98), mode="bilinear",
                              align_corners=False)
        features  = self.backbone(x)           # [B, 384]  CLS token via timm
        projected = self.projection(features)  # [B, output_dim]
        return projected


class NoMaD_ViNT_DINOv2(nn.Module):
    """
    使用 DINOv2 替换 EfficientNet 的 NoMaD 视觉编码器。
    保持与原始 NoMaD_ViNT 相同的输入/输出接口。
    """

    def __init__(
        self,
        context_size: int = 3,
        obs_encoding_size: int = 256,
        mha_num_attention_heads: int = 4,
        mha_num_attention_layers: int = 4,
        mha_ff_dim_factor: int = 4,
        freeze_backbone: bool = True,
    ):
        super().__init__()
        self.context_size = context_size
        self.obs_encoding_size = obs_encoding_size

        # ── 图像编码器 ──
        self.obs_encoder  = DINOv2Encoder(obs_encoding_size, freeze_backbone)
        self.goal_encoder = DINOv2Encoder(obs_encoding_size, freeze_backbone)

        # 目标融合: concat(obs_feat, goal_feat) -> 融合特征
        self.goal_fusion = nn.Sequential(
            nn.Linear(obs_encoding_size * 2, obs_encoding_size),
            nn.GELU(),
        )

        # ── 时序自注意力（与原始结构相同）──
        from vint_train.models.vint.self_attention import PositionalEncoding

        self.positional_encoding = PositionalEncoding(
            obs_encoding_size, max_seq_len=context_size + 2,
        )
        sa_layer = nn.TransformerEncoderLayer(
            d_model=obs_encoding_size,
            nhead=mha_num_attention_heads,
            dim_feedforward=mha_ff_dim_factor * obs_encoding_size,
            activation="gelu",
            batch_first=True,
            norm_first=True,
        )
        self.sa_encoder = nn.TransformerEncoder(
            sa_layer, num_layers=mha_num_attention_layers,
        )

        # ── 目标遮罩（与原始结构相同）──
        self.goal_mask = torch.zeros((1, context_size + 2), dtype=torch.bool)
        self.goal_mask[:, -1] = True
        self.no_mask   = torch.zeros((1, context_size + 2), dtype=torch.bool)
        self.all_masks = torch.cat([self.no_mask, self.goal_mask], dim=0)
        self.avg_pool_mask = torch.cat([
            1 - self.no_mask.float(),
            (1 - self.goal_mask.float())
            * ((context_size + 2) / (context_size + 1)),
        ], dim=0)

    def forward(self, obs_img, goal_img, input_goal_mask=None):
        device = obs_img.device

        # 将 [B, (C+1)*3, H, W] 拆分为 C+1 张图像
        obs_imgs_split = torch.split(obs_img, 3, dim=1)
        obs_encodings  = []
        for img in obs_imgs_split:
            enc = self.obs_encoder(img)             # [B, 256]
            obs_encodings.append(enc.unsqueeze(1))  # [B, 1, 256]
        obs_encoding = torch.cat(obs_encodings, dim=1)  # [B, C+1, 256]

        # 编码目标
        last_obs  = obs_imgs_split[-1]
        obs_feat  = self.obs_encoder(last_obs)      # [B, 256]
        goal_feat = self.goal_encoder(goal_img)     # [B, 256]
        goal_encoding = self.goal_fusion(
            torch.cat([obs_feat, goal_feat], dim=-1)
        ).unsqueeze(1)                              # [B, 1, 256]

        tokens = torch.cat([obs_encoding, goal_encoding], dim=1)  # [B, C+2, 256]

        # 目标遮罩
        if input_goal_mask is not None:
            idx = input_goal_mask.long()
            src_key_padding_mask = torch.index_select(
                self.all_masks.to(device), 0, idx,
            )
        else:
            src_key_padding_mask = None

        # 位置编码 + 自注意力
        tokens = self.positional_encoding(tokens)
        tokens = self.sa_encoder(
            tokens, src_key_padding_mask=src_key_padding_mask,
        )

        # 加权平均池化
        if src_key_padding_mask is not None:
            avg_mask = torch.index_select(
                self.avg_pool_mask.to(device), 0, idx,
            ).unsqueeze(-1)
            tokens = tokens * avg_mask

        return torch.mean(tokens, dim=1)            # [B, 256]
```

> **📁 文件放置**：将上述代码保存到远程训练机的  
> `/home/yifei/codespace/visualnav-transformer/scripts/nomad_vint_dinov2.py`  
> **说明**：本文件是 DINOv2 视觉编码器的适配模块，供 Day 4 训练脚本导入使用。

### 4.3 创建完整训练配置文件

创建 `train/config/nomad_dinov2.yaml`（**完整可用**，非片段）：

```yaml
# ────────────────────────────────────────────────────────────
# NoMaD + DINOv2-Small 训练配置
# 基于 nomad.yaml，做了以下修改：
#   1. vision_encoder: nomad_vint_dinov2（替换 nomad_vint）
#   2. batch_size: 64（DINOv2-S 21M 参数 → 每样本显存占用更高）
#   3. lr: 5e-5（主干冻结，仅投影层 + 扩散头可训练）
#   4. epochs: 20（短期验证运行；收敛后可扩展至 100）
#   5. image_size: [98, 98]（DINOv2 patch_size=14, 98=14×7）
# ────────────────────────────────────────────────────────────

project_name: nomad-dinov2
run_name: nomad_dinov2

# ── 训练设置 ──────────────────────────────────────────────
use_wandb: True
train: True
batch_size: 64          # 从 256 减小：DINOv2 主干占用更多显存
eval_batch_size: 64
epochs: 20              # 短期验证；确认收敛后可增至 100
gpu_ids: [0]
num_workers: 8          # 从 12 减少，避免小 batch 下 CPU 瓶颈
lr: 5e-5               # 较低学习率：主干冻结，仅训练投影层 + 头部
optimizer: adamw
clipping: False
max_norm: 1.0
scheduler: "cosine"
warmup: True
warmup_epochs: 2        # 20 轮训练的较短预热
cyclic_period: 10
plateau_patience: 3
plateau_factor: 0.5
seed: 0

# ── 模型架构 ──────────────────────────────────────────────
model_type: nomad
vision_encoder: nomad_vint_dinov2   # <-- DINOv2 编码器（scripts/nomad_vint_dinov2.py）
encoding_size: 256
obs_encoder: dinov2_vits14          # 信息标注：实际加载在 NoMaD_ViNT_DINOv2 中
freeze_backbone: True               # 冻结 DINOv2，仅训练投影层
attn_unet: False
cond_predict_scale: False
mha_num_attention_heads: 4
mha_num_attention_layers: 4
mha_ff_dim_factor: 4
down_dims: [64, 128, 256]

# ── 扩散模型 ──────────────────────────────────────────────
num_diffusion_iters: 10

# ── 目标遮罩 ──────────────────────────────────────────────
goal_mask_prob: 0.5

# ── 数据 ────────────────────────────────────────────────────
normalize: True
context_type: temporal
context_size: 3
alpha: 1e-4

distance:
  min_dist_cat: 0
  max_dist_cat: 20
action:
  min_dist_cat: 3
  max_dist_cat: 20

len_traj_pred: 8
learn_angle: False

image_size: [98, 98]    # DINOv2 patch_size=14, 98=14×7（从 96×96 修改）

datasets:
  go_stanford:
    data_folder: /home/yifei/nomad_dataset/go_stanford_cropped
    train: /home/yifei/data_splits/go_stanford/train/
    test: /home/yifei/data_splits/go_stanford/test/
    end_slack: 0
    goals_per_obs: 2
    negative_mining: True

# ── 日志 ──────────────────────────────────────────────────
print_log_freq: 50      # 短期训练更频繁记录日志
image_log_freq: 500
num_images_log: 8
pairwise_test_freq: 0
eval_fraction: 0.25
wandb_log_freq: 10
eval_freq: 1
```

> **📁 文件放置**：保存到训练机  
> `/home/yifei/codespace/visualnav-transformer/train/config/nomad_dinov2.yaml`

### 4.4 编写独立训练脚本

原版 `train.py` 中 vision encoder 创建逻辑是硬编码的（仅支持 `nomad_vint` / `vib` / `vit`），
我们**不修改原文件**，而是创建独立的 `scripts/train_dinov2.py`，
复用原有数据加载和训练循环，仅替换 vision encoder 构建部分。

创建 `scripts/train_dinov2.py`：

```python
#!/usr/bin/env python3
"""
Day 4: NoMaD + DINOv2 训练脚本

独立训练脚本，将 EfficientNet-B0 替换为 DINOv2-Small 作为视觉编码器。
复用原始训练基础设施（ViNT_Dataset, train_eval_loop_nomad,
DDPMScheduler, ConditionalUnet1D）。

与原始 train.py 的主要区别:
  1. 视觉编码器: NoMaD_ViNT_DINOv2 (scripts/nomad_vint_dinov2.py)
  2. 图像尺寸: 98×98 (DINOv2 patch_size=14, 98=14×7)
  3. 默认冻结主干 → 仅训练投影层 + 扩散头
  4. 学习率: 5e-5 (冻结主干微调时降低)

用法:
  cd /home/yifei/codespace/visualnav-transformer/train
  python ../scripts/train_dinov2.py -c config/nomad_dinov2.yaml
"""

import os
import sys
import time
import argparse

import yaml
import numpy as np
import wandb

import torch
import torch.nn as nn
from torch.utils.data import DataLoader, ConcatDataset
from torch.optim import AdamW
from torchvision import transforms
import torch.backends.cudnn as cudnn
from warmup_scheduler import GradualWarmupScheduler

from diffusers.schedulers.scheduling_ddpm import DDPMScheduler

# ── 项目设置 ──────────────────────────────────────────────
PROJECT_ROOT = "/home/yifei/codespace/visualnav-transformer"
sys.path.insert(0, os.path.join(PROJECT_ROOT, "train"))
sys.path.insert(0, os.path.join(PROJECT_ROOT, "scripts"))

from vint_train.models.nomad.nomad import NoMaD, DenseNetwork
from vint_train.models.nomad.nomad_vint import replace_bn_with_gn
from diffusion_policy.model.diffusion.conditional_unet1d import ConditionalUnet1D
from vint_train.data.vint_dataset import ViNT_Dataset
from vint_train.training.train_eval_loop import train_eval_loop_nomad

# DINOv2 视觉编码器 (来自 scripts/nomad_vint_dinov2.py)
from nomad_vint_dinov2 import NoMaD_ViNT_DINOv2


def build_dinov2_model(config: dict) -> tuple:
    """
    构建使用 DINOv2 视觉编码器的 NoMaD 模型。

    返回:
        model: NoMaD 模型
        noise_scheduler: DDPMScheduler
        trainable_params: 可训练参数数量
    """
    freeze = config.get("freeze_backbone", True)

    # 1) 视觉编码器 — DINOv2
    vision_encoder = NoMaD_ViNT_DINOv2(
        context_size=config["context_size"],
        obs_encoding_size=config["encoding_size"],
        mha_num_attention_heads=config["mha_num_attention_heads"],
        mha_num_attention_layers=config["mha_num_attention_layers"],
        mha_ff_dim_factor=config["mha_ff_dim_factor"],
        freeze_backbone=freeze,
    )
    vision_encoder = replace_bn_with_gn(vision_encoder)

    # 2) 噪声预测网络（与原始架构相同）
    noise_pred_net = ConditionalUnet1D(
        input_dim=2,
        global_cond_dim=config["encoding_size"],
        down_dims=config["down_dims"],
        cond_predict_scale=config.get("cond_predict_scale", False),
    )

    # 3) 距离预测网络（与原始结构相同）
    dist_pred_net = DenseNetwork(embedding_dim=config["encoding_size"])

    # 4) 组装 NoMaD
    model = NoMaD(
        vision_encoder=vision_encoder,
        noise_pred_net=noise_pred_net,
        dist_pred_net=dist_pred_net,
    )

    # 5) 噪声调度器
    noise_scheduler = DDPMScheduler(
        num_train_timesteps=config["num_diffusion_iters"],
        beta_schedule="squaredcos_cap_v2",
        clip_sample=True,
        prediction_type="epsilon",
    )

    # 统计可训练参数
    total = sum(p.numel() for p in model.parameters())
    trainable = sum(p.numel() for p in model.parameters() if p.requires_grad)
    print(f"\n[Model] Total params: {total:,}")
    print(f"[Model] Trainable params: {trainable:,} "
          f"({100*trainable/total:.1f}%)")
    if freeze:
        print("[Model] DINOv2 backbone FROZEN — only projection + heads trained")
    else:
        print("[Model] DINOv2 backbone UNFROZEN — full fine-tuning")

    return model, noise_scheduler, trainable


def build_dataloaders(config: dict):
    """根据配置构建训练和测试数据加载器。"""
    transform = transforms.Compose([
        transforms.Normalize(
            mean=[0.485, 0.456, 0.406],
            std=[0.229, 0.224, 0.225],
        ),
    ])

    train_datasets = []
    test_dataloaders = {}

    for ds_name, ds_cfg in config["datasets"].items():
        ds_cfg.setdefault("negative_mining", True)
        ds_cfg.setdefault("goals_per_obs", 1)
        ds_cfg.setdefault("end_slack", 0)
        ds_cfg.setdefault("waypoint_spacing", 1)

        for split in ["train", "test"]:
            if split not in ds_cfg:
                continue
            dataset = ViNT_Dataset(
                data_folder=ds_cfg["data_folder"],
                data_split_folder=ds_cfg[split],
                dataset_name=ds_name,
                image_size=config["image_size"],
                waypoint_spacing=ds_cfg["waypoint_spacing"],
                min_dist_cat=config["distance"]["min_dist_cat"],
                max_dist_cat=config["distance"]["max_dist_cat"],
                min_action_distance=config["action"]["min_dist_cat"],
                max_action_distance=config["action"]["max_dist_cat"],
                negative_mining=ds_cfg["negative_mining"],
                len_traj_pred=config["len_traj_pred"],
                learn_angle=config["learn_angle"],
                context_size=config["context_size"],
                context_type=config.get("context_type", "temporal"),
                end_slack=ds_cfg["end_slack"],
                goals_per_obs=ds_cfg["goals_per_obs"],
                normalize=config["normalize"],
                goal_type=config.get("goal_type", "image"),
            )
            if split == "train":
                train_datasets.append(dataset)
            else:
                test_dataloaders[f"{ds_name}_test"] = dataset

    train_dataset = ConcatDataset(train_datasets)
    train_loader = DataLoader(
        train_dataset,
        batch_size=config["batch_size"],
        shuffle=True,
        num_workers=config["num_workers"],
        drop_last=False,
        persistent_workers=True,
    )

    eval_bs = config.get("eval_batch_size", config["batch_size"])
    for key in test_dataloaders:
        test_dataloaders[key] = DataLoader(
            test_dataloaders[key],
            batch_size=eval_bs,
            shuffle=True,
            num_workers=0,
            drop_last=False,
        )

    print(f"[Data] Train samples: {len(train_dataset):,}")
    for k, v in test_dataloaders.items():
        print(f"[Data] Test '{k}': {len(v.dataset):,} samples")

    return train_loader, test_dataloaders, transform


def main(config: dict):
    """训练主入口。"""
    # ── 设备 ──
    if torch.cuda.is_available():
        os.environ["CUDA_DEVICE_ORDER"] = "PCI_BUS_ID"
        gpu_ids = config.get("gpu_ids", [0])
        os.environ["CUDA_VISIBLE_DEVICES"] = ",".join(str(g) for g in gpu_ids)
        device = torch.device(f"cuda:{gpu_ids[0]}")
        print(f"[Device] CUDA: {torch.cuda.get_device_name(gpu_ids[0])}")
    else:
        device = torch.device("cpu")
        print("[Device] CPU (no CUDA)")

    if "seed" in config:
        np.random.seed(config["seed"])
        torch.manual_seed(config["seed"])
        cudnn.deterministic = True
    cudnn.benchmark = True

    # ── 构建模型 ──
    model, noise_scheduler, n_trainable = build_dinov2_model(config)

    # ── 梯度裁剪 ──
    if config.get("clipping", False):
        max_norm = config.get("max_norm", 1.0)
        print(f"[Train] Gradient clipping: max_norm={max_norm}")
        for p in model.parameters():
            if p.requires_grad:
                p.register_hook(
                    lambda grad, mn=max_norm: torch.clamp(grad, -mn, mn)
                )

    # ── 优化器 ──
    lr = float(config["lr"])
    optimizer = AdamW(
        filter(lambda p: p.requires_grad, model.parameters()),
        lr=lr,
    )
    print(f"[Train] Optimizer: AdamW, lr={lr}")

    # ── 学习率调度器 ──
    scheduler = None
    sched_name = config.get("scheduler")
    if sched_name == "cosine":
        scheduler = torch.optim.lr_scheduler.CosineAnnealingLR(
            optimizer, T_max=config["epochs"],
        )
        print(f"[Train] Scheduler: CosineAnnealing, T_max={config['epochs']}")
    elif sched_name == "plateau":
        scheduler = torch.optim.lr_scheduler.ReduceLROnPlateau(
            optimizer,
            factor=config.get("plateau_factor", 0.5),
            patience=config.get("plateau_patience", 3),
        )

    if config.get("warmup", False):
        warmup_epochs = config.get("warmup_epochs", 4)
        scheduler = GradualWarmupScheduler(
            optimizer, multiplier=1,
            total_epoch=warmup_epochs,
            after_scheduler=scheduler,
        )
        print(f"[Train] Warmup: {warmup_epochs} epochs")

    # ── 数据 ──
    train_loader, test_dataloaders, transform = build_dataloaders(config)

    # ── 多 GPU ──
    gpu_ids = config.get("gpu_ids", [0])
    if len(gpu_ids) > 1:
        model = nn.DataParallel(model, device_ids=gpu_ids)
    model = model.to(device)

    # ── 训练 ──
    print(f"\n{'='*60}")
    print(f"Starting DINOv2 Training: {config['epochs']} epochs, "
          f"batch_size={config['batch_size']}")
    print(f"{'='*60}\n")

    train_eval_loop_nomad(
        train_model=config["train"],
        model=model,
        optimizer=optimizer,
        lr_scheduler=scheduler,
        noise_scheduler=noise_scheduler,
        train_loader=train_loader,
        test_dataloaders=test_dataloaders,
        transform=transform,
        goal_mask_prob=config["goal_mask_prob"],
        epochs=config["epochs"],
        device=device,
        project_folder=config["project_folder"],
        print_log_freq=config["print_log_freq"],
        wandb_log_freq=config["wandb_log_freq"],
        image_log_freq=config["image_log_freq"],
        num_images_log=config["num_images_log"],
        current_epoch=0,
        alpha=float(config["alpha"]),
        use_wandb=config["use_wandb"],
        eval_fraction=config["eval_fraction"],
        eval_freq=config["eval_freq"],
    )

    print("\n[DONE] DINOv2 training complete!")
    print(f"  Checkpoints saved to: {config['project_folder']}")
    print(f"  Best model: look for lowest test loss in wandb, use ema_<epoch>.pth")


if __name__ == "__main__":
    torch.multiprocessing.set_start_method("spawn")

    parser = argparse.ArgumentParser(
        description="NoMaD + DINOv2 训练脚本",
    )
    parser.add_argument(
        "--config", "-c",
        default="config/nomad_dinov2.yaml",
        type=str,
        help="YAML 配置文件路径",
    )
    args = parser.parse_args()

    # 加载默认配置 + 用户配置
    defaults_path = os.path.join(PROJECT_ROOT, "train/config/defaults.yaml")
    with open(defaults_path, "r") as f:
        config = yaml.safe_load(f)

    with open(args.config, "r") as f:
        user_config = yaml.safe_load(f)
    config.update(user_config)

    # 创建输出目录
    config["run_name"] += "_" + time.strftime("%Y_%m_%d_%H_%M_%S")
    config["project_folder"] = os.path.join(
        PROJECT_ROOT, "train", "logs",
        config["project_name"], config["run_name"],
    )
    os.makedirs(config["project_folder"], exist_ok=True)

    # 保存配置快照
    cfg_path = os.path.join(config["project_folder"], "config.yaml")
    with open(cfg_path, "w") as f:
        yaml.dump(config, f, default_flow_style=False, sort_keys=False)
    print(f"Config saved: {cfg_path}")

    # 初始化 wandb
    if config.get("use_wandb", False):
        wandb.login()
        wandb.init(
            project=config["project_name"],
            settings=wandb.Settings(start_method="fork"),
        )
        wandb.run.name = config["run_name"]
        wandb.config.update(config)
        wandb.save(args.config, policy="now")

    main(config)
```

> **📁 文件放置**：将上述代码保存到远程训练机的  
> `/home/yifei/codespace/visualnav-transformer/scripts/train_dinov2.py`  
> **依赖**：需要 `scripts/nomad_vint_dinov2.py`（§4.2）已放置在同目录  
> **✅ 运行方式**：
>
> ```bash
> cd /home/yifei/codespace/visualnav-transformer/train
> python ../scripts/train_dinov2.py -c config/nomad_dinov2.yaml
> ```

### 4.5 短期训练验证流程（≈ 3 h GPU）

**步骤 1**：确认数据集路径正确

```bash
# 检查 data_splits 是否存在（训练时 ViNT_Dataset 从中读取 traj_names.txt）
ls /home/yifei/data_splits/go_stanford/train/traj_names.txt
ls /home/yifei/data_splits/go_stanford/test/traj_names.txt

# 如果不存在，使用 train/data_split.py 生成
cd /home/yifei/codespace/visualnav-transformer/train
python data_split.py --data_folder /home/yifei/nomad_dataset/go_stanford_cropped \
                     --output_folder /home/yifei/data_splits/go_stanford \
                     --train_ratio 0.8
```

**步骤 2**：启动训练

```bash
cd /home/yifei/codespace/visualnav-transformer/train
python ../scripts/train_dinov2.py -c config/nomad_dinov2.yaml
```

**步骤 3**：监控训练

```bash
# 在另一个终端查看 wandb 面板或查看日志
# 关注指标：
#   - train_loss: 应在前 5 epoch 显著下降
#   - test_loss: 应与 EfficientNet 基线版本趋势一致
#   - 如果 loss 在 epoch 5 之后仍不下降 → 见 §4.6 故障排除
```

**步骤 4**：导出最佳权重

```bash
# 假设 epoch 15 的 test loss 最低
DINOV2_RUN="logs/nomad-dinov2/nomad_dinov2_2026_03_27_10_00_00"

mkdir -p /home/yifei/codespace/visualnav-transformer/deployment/model_weights/nomad_dinov2
cp /home/yifei/codespace/visualnav-transformer/train/${DINOV2_RUN}/ema_15.pth \
   /home/yifei/codespace/visualnav-transformer/deployment/model_weights/nomad_dinov2/nomad_dinov2.pth
```

### 4.6 故障排除 & 进阶策略

| 现象 | 原因 | 解决方案 |
|------|------|---------|
| Loss 不下降 | 学习率过低 / 冻结过多 | 解冻 DINOv2 最后 2 层：修改 `nomad_vint_dinov2.py` 的 `freeze_backbone` 逻辑 |
| OOM (显存不足) | batch_size 过大 | 降低 `batch_size: 32` 或使用梯度累积 |
| Loss 震荡不收敛 | 学习率过高 | 降低 `lr: 1e-5` |
| Test loss 上升 | 过拟合 | 增加 `goal_mask_prob: 0.6` 作为正则化 |

**进阶：解冻 DINOv2 最后 N 层**

如果冻结全部 backbone 不收敛，可选择性解冻最后几层：

```python
# 在 nomad_vint_dinov2.py 的 DINOv2Encoder.__init__ 中修改：
if freeze_backbone:
    for name, p in self.backbone.named_parameters():
        # 解冻最后 2 个 transformer 块
        if "blocks.10" in name or "blocks.11" in name or "norm" in name:
            p.requires_grad = True
        else:
            p.requires_grad = False
    print("[DINOv2] Backbone partially frozen — last 2 blocks + norm trainable")
```

### 4.7 Day 4 验收清单

- [ ] DINOv2-Small 权重下载成功（`python -c "import timm; timm.create_model('vit_small_patch14_dinov2.lvd142m', pretrained=True)"`）
- [ ] `nomad_dinov2.yaml` 配置文件创建完成
- [ ] `train_dinov2.py` 训练脚本可运行
- [ ] 适配代码维度匹配正确（`forward()` 输出 `[B, 256]`）
- [ ] 短期训练（20 epoch）完成
- [ ] wandb 记录了 loss 曲线与 EfficientNet 基线对比
- [ ] 判断 DINOv2 可行性（收敛 / 不收敛 → 对应不同的 Day 7 论文叙事）
- [ ] 如不收敛：尝试解冻最后 2 层并重跑
- [ ] **分析了 DINOv2 特征对视角差异的鲁棒性是否优于 EfficientNet（跨形态价值）**

---

## Day 5（周五）：足式机器人仿真适配 + 轮式 vs 足式对比（Ubuntu 20.04）

> ⚠️ **此部分在 Ubuntu 20.04 仿真机器上操作**，不在训练机上运行。

### 5.1 安装仿真环境（≈ 30 min）

```bash
# 在 Ubuntu 20.04 仿真机上执行
pip install pybullet numpy matplotlib

# 验证
python -c "import pybullet as p; print('PyBullet version:', p.getAPIVersion())"
```

### 5.2 下载 DeepRobotics Lite3 URDF

```bash
mkdir -p /home/yifei/codespace/visualnav-transformer/assets/lite3

# 从云深处科技获取 Lite3 URDF
# 方式一：从 DeepRobotics 官方仓库
git clone https://github.com/DeepRoboticsLab/Lite3_Model_Control.git /tmp/lite3_repo
cp -r /tmp/lite3_repo/lite3_description /home/yifei/codespace/visualnav-transformer/assets/lite3/

# 方式二：若仓库结构不同，手动下载 URDF 文件
# 将 lite3.urdf + meshes/ 放入 assets/lite3/ 目录
```

### 5.3 编写仿真控制脚本

创建 `scripts/lite3_sim.py`：

```python
#!/usr/bin/env python3
"""
Day 5: 四足机器人仿真适配器（Ubuntu 20.04）
将 NoMaD 路径点通过 PD 控制器转换为速度指令并在 PyBullet 中执行。

接口链:
  NoMaD 路径点 (dx, dy)
    -> waypoint_to_velocity -> (v, ω)
    -> PyBullet -> 机器人运动
"""

import os
import numpy as np
import time

import pybullet as p
import pybullet_data


class Lite3Simulator:
    """DeepRobotics Lite3 / Husky（回退）仿真控制器"""

    def __init__(self, urdf_path: str = None, gui: bool = True):
        mode = p.GUI if gui else p.DIRECT
        self.client = p.connect(mode)
        p.setAdditionalSearchPath(pybullet_data.getDataPath())
        p.setGravity(0, 0, -9.81)

        self.plane = p.loadURDF("plane.urdf")

        if urdf_path and os.path.exists(urdf_path):
            self.robot = p.loadURDF(urdf_path, [0, 0, 0.4],
                                    useFixedBase=False)
            print(f"[Lite3Sim] URDF loaded: {urdf_path}")
        else:
            # 回退到 PyBullet 内置的 Husky
            self.robot = p.loadURDF("husky/husky.urdf", [0, 0, 0.1],
                                    useFixedBase=False)
            print("[Lite3Sim] WARNING: Lite3 URDF not found, using Husky as fallback")

        self.dt = 1.0 / 240.0

    def get_pose(self):
        """返回 (x, y) 坐标和 yaw 偏航角"""
        pos, orn = p.getBasePositionAndOrientation(self.robot)
        yaw = p.getEulerFromQuaternion(orn)[2]
        return np.array(pos[:2]), yaw

    def set_velocity(self, v: float, w: float):
        """设置线速度 v (m/s) 和角速度 w (rad/s)"""
        p.resetBaseVelocity(
            self.robot,
            linearVelocity=[v, 0, 0],
            angularVelocity=[0, 0, w],
        )

    def step(self, n: int = 1):
        for _ in range(n):
            p.stepSimulation()

    def close(self):
        p.disconnect()


def waypoint_to_velocity(
    waypoint: np.ndarray,
    dt: float = 0.25,    # NoMaD 帧率=4 → dt=0.25s
    max_v: float = 0.5,
    max_w: float = 1.0,
) -> tuple:
    """
    将 NoMaD 路径点 (dx, dy) 转换为速度指令 (v, ω)。
    逻辑与 deployment/src/pd_controller.py 对齐。
    """
    dx, dy = float(waypoint[0]), float(waypoint[1])
    EPS = 1e-8

    if abs(dx) < EPS and abs(dy) < EPS:
        return 0.0, 0.0
    elif abs(dx) < EPS:
        v = 0.0
        w = np.sign(dy) * np.pi / (2 * dt)
    else:
        v = dx / dt
        w = np.arctan(dy / dx) / dt

    v = float(np.clip(v, 0, max_v))
    w = float(np.clip(w, -max_w, max_w))
    return v, w


def demo_open_loop():
    """开环演示: 使用预设路径点序列驱动机器人"""
    sim = Lite3Simulator(gui=True)

    waypoints = np.array([
        [0.05,  0.01], [0.10,  0.02], [0.15,  0.01], [0.20, -0.01],
        [0.25, -0.02], [0.30,  0.00], [0.35,  0.01], [0.40,  0.00],
    ])

    positions = []
    pos, yaw = sim.get_pose()
    positions.append(pos.copy())

    print("Starting simulation (open-loop)...")
    for i, wp in enumerate(waypoints):
        v, w = waypoint_to_velocity(wp)
        print(f"  WP {i}: ({wp[0]:.3f}, {wp[1]:.3f}) -> v={v:.3f}, w={w:.3f}")

        for _ in range(100):       # 100 steps ≈ 0.42 s
            sim.set_velocity(v, w)
            sim.step()
            time.sleep(sim.dt)

        pos, yaw = sim.get_pose()
        positions.append(pos.copy())

    positions = np.array(positions)
    print(f"\nStart: ({positions[0][0]:.3f}, {positions[0][1]:.3f})")
    print(f"End: ({positions[-1][0]:.3f}, {positions[-1][1]:.3f})")
    print(f"Total Displacement: {np.linalg.norm(positions[-1] - positions[0]):.3f} m")

    input("Press Enter to close simulation...")
    sim.close()


if __name__ == "__main__":
    demo_open_loop()
```

> **📁 文件放置**：将上述代码保存到 **仿真机（Ubuntu 20.04）** 的  
> `/home/yifei/codespace/visualnav-transformer/scripts/lite3_sim.py`  
> **✅ 运行方式**（在仿真机上执行）：
>
> ```bash
> cd /home/yifei/codespace/visualnav-transformer
> python scripts/lite3_sim.py
> ```

**📊 指标输出说明**（对应 `results/day5/.../lite3_open_loop.txt`）：

| 指标 | 含义 | 如何解读 |
| ---- | ---- | -------- |
| `cmd_v` / `cmd_w` | PD 控制器将 waypoint 转换后的线速度/角速度指令 | 验证 waypoint→velocity 映射是否合理 |
| `pos_x` / `pos_y` | 每步执行后机器人世界坐标 | 轨迹是否沿期望方向运动 |
| `Total Displacement` | 起点到终点的欧氏距离 | >0 说明机器人成功按指令运动 |

> 💡 **对比实验**（`python scripts/lite3_sim.py --compare`）的核心指标：
>
> | 指标 | 含义 |
> | ---- | ---- |
> | `Path Length` | 沿轨迹总行驶距离 (m)；足式可能因姿态调整而更长 |
> | `Final Displacement` | 起→终直线距离；对比轮式/足式到达效率 |
> | `Lateral Deviation` | 平均侧向偏差 (m)；**核心差异指标** — 足式偏差越大说明迁移越困难 |

### 5.4 对接 NoMaD 推理（闭环验证思路）

完整闭环需要在仿真中截取相机画面：

```
仿真渲染 RGB 图像
  → 送入 NoMaD 推理 → waypoint
  → PD 控制器 → (v, ω)
  → 仿真执行
  → 循环
```

> 如果 Day 5 时间不够，先做 **开环验证**：用数据集图像序列推理出速度指令，
> 在仿真中回放运动轨迹。

### 5.5 轮式 vs 足式对比实验（下午 ≈ 2 h）

> **科研空白 G1+G2**：直接回答“轮式训练数据能否迁移到足式？”

**实验设计**：用相同的 NoMaD 权重 + 相同的 waypoint 序列，
分别驱动 Husky（轮式）和 Lite3（足式）在仿真中执行：

```python
# 对比实验，位于 lite3_sim.py 中
def compare_wheeled_vs_legged():
    """
    科研实验：相同路径点序列在轮式与足式平台上的执行对比。

    指标:
      - 到达率: 足式 vs 轮式
      - 路径偏差: 实际轨迹与路径点之间的 MSE
      - 执行时间: 足式是否因稳定性问题而耗时更长
      - 失败模式: 足式特有的失败（跌倒、不稳定）
    """
    waypoints = np.array([
        [0.05, 0.01], [0.10, 0.02], [0.15, 0.01], [0.20, -0.01],
        [0.25, -0.02], [0.30, 0.00], [0.35, 0.01], [0.40, 0.00],
    ])

    for platform in ["wheeled", "legged"]:
        if platform == "wheeled":
            sim = Lite3Simulator(urdf_path=None, gui=False)  # 默认 Husky
            max_v, max_w = 0.2, 0.4  # 与 NoMaD 原始 PD 控制器一致
        else:
            sim = Lite3Simulator(
                urdf_path="/home/yifei/codespace/visualnav-transformer/assets/lite3/lite3.urdf",
                gui=False,
            )
            max_v, max_w = 0.5, 1.0  # 足式限制更宽松

        positions = []
        pos, _ = sim.get_pose()
        positions.append(pos.copy())

        for wp in waypoints:
            v, w = waypoint_to_velocity(wp, max_v=max_v, max_w=max_w)
            for _ in range(100):
                sim.set_velocity(v, w)
                sim.step()
            pos, _ = sim.get_pose()
            positions.append(pos.copy())

        sim.close()
        # ... 记录指标，对比分析
```

**预期观察**：
- 轮式路径偏差更小（差速驱动更精确）
- 足式可能出现失稳/偏移（尤其急转弯 waypoint）
- 这个对比**直接回答“为什么足式部署更难”的科研问题**

---

### 5.6 Day 5 验收清单

- [ ] PyBullet 安装成功，能显示 GUI 仿真窗口
- [ ] Lite3 URDF 加载成功（或 Husky 替代模型可用）
- [ ] `waypoint_to_velocity()` 与 `pd_controller.py` 逻辑一致
- [ ] 开环仿真演示成功，机器人按 waypoint 移动
- [ ] **完成轮式 vs 足式对比实验，记录了路径偏差、失败模式**
- [ ] **生成了跨形态对比图表（论文图 5 素材）**
- [ ] 记录了仿真截图 / 录屏

---

## Day 6（周六）：面向足式部署的整合消融研究

### 6.1 消融实验矩阵（含足式特有维度）

> 消融研究不仅要对比技术改进，更要回答：**每项改进对足式部署的贡献是什么？**

| 编号 | 采样器 | 编码器 | CFG (w) | 步态抖动 | 足式意义 |
|------|--------|--------|---------|---------|----------|
| C1 | DDPM-10 | EffNet | 0 | 无 | **基线** |
| C2 | DDIM-5 | EffNet | 0 | 无 | 控制频率提升 (§G3) |
| C3 | DDIM-3 | EffNet | 0 | 无 | 极限加速 |
| C4 | DDPM-10 | EffNet | 1.0 | 无 | 目标引导增强 |
| C5 | DDIM-5 | EffNet | 1.0 | 无 | **推荐足式配置** |
| C6 | DDIM-3 | EffNet | 1.0 | 无 | 极限加速 + CFG |
| C7 | DDPM-10 | EffNet | 2.0 | 无 | 强引导对比 |
| C8 | DDIM-5 | EffNet | 2.0 | 无 | 强引导 + 加速 |
| ... | 3×3×4 | ... | ... | 轻微/中等/严重 | 共 36 组配置 |

> 完整 3×3×4 = 36 组配置（采样器: DDPM-10 / DDIM-5 / DDIM-3 × CFG: w=0 / 1.0 / 2.0 × 抖动: 无 / 轻微 / 中等 / 严重）由 `scripts/ablation_experiment.py` 统一执行。

**关键对比线**：
- C1 vs 抖动组 → 步态抖动对导航的影响有多大？
- 抖动组 w=0 vs w=1.0 → CFG 能否缓解抖动干扰？
- C1 vs C5 → DDIM+CFG 组合的综合效果
- C5 vs 抖动 C5 → 最佳配置在抖动下是否仍然最优？
- DINOv2 组（可选）→ 跨形态泛化价值

> 每个配置在 **≥ 3 条不同轨迹** 上测试。

### 6.2 编写统一评估脚本

创建 `scripts/ablation_experiment.py`，功能包括：

1. 遍历所有 3×3×4 = 36 组配置组合
2. 在多条轨迹上运行推理（含 Lite3 步态抖动参数）
3. 记录推理时间、轨迹质量（MSE vs 基线）、多样性
4. 生成对比表格（txt + Markdown）和图表（热力图、雷达图、Pareto 前沿、抖动鲁棒性曲线）
5. 自动输出 `ablation_full_results.txt` + `ablation_summary.txt` 并解释每列数据意义
6. 保存到 `results/day6/`

**📊 消融实验指标输出说明**（对应 `ablation_full_results.txt` 各列）：

| 指标 | 含义 | 足式部署判断标准 |
| ---- | ---- | ---------------- |
| `inference_ms` | 单次推理延迟 (ms) | ≤67 ms → 频率 ≥15 Hz（四足导航门槛） |
| `diversity` | 轨迹端点标准差 | >0.02 保留扩散多模态优势 |
| `mse_vs_clean` | 与无抖动基线的均方误差 | <0.01 可接受 |
| `cos_sim` | 方向余弦相似度 | >0.95 鲁棒 / <0.90 需补偿 |
| `heading_dev` | 航向偏差 (°) | <5° 安全 / >10° 危险 |
| `mean_disp` | 平均位移（目标导向性） | 越大越好（表示积极朝目标移动） |

> 💡 **如何选取最佳配置**：在 Pareto 前沿图中，选取 `cos_sim > 0.94` 且 `inference_ms < 70` 的帕累托最优点。
> 典型推荐配置为 **DDIM-5 + CFG w=1.0**。

---

### 6.3 Day 6 验收清单

- [ ] 完成所有配置的消融实验（36 组，含抖动维度）
- [ ] 生成对比表格（txt + Markdown 格式）
- [ ] 确定最佳配置组合
- [ ] **明确回答了“CFG 能否补偿抖动”的科研问题**
- [ ] **生成了可直接放入论文的消融对比图表**
- [ ] 所有实验数据保存到 `results/day6/`

---

## Day 7（周日）：论文叙事构建 + 研究贡献总结

### 7.1 实验报告撰写（围绕科研问题）

在项目根目录下创建 `实验报告.md`，**每一节必须回答一个科研问题**：

| 章节 | 回答的问题 | 对应实验 |
|------|-----------|--------|
| §1 引言 | 为什么要将扩散导航部署到足式机器人？ | 文献空白分析 |
| §2 相关工作 | 现有工作解决了什么、留下了什么？ | Day 1 文献梳理 |
| §3 方法 | NoMaD 架构 + 我们的改进点 | DDIM/CFG/DINOv2 |
| §4.1 | DDIM 加速能否满足足式控制频率？ | Day 2 |
| §4.2 | CFG 能否增强目标导向性？ | Day 3 上午 |
| §4.3 | 步态抖动如何影响视觉导航？CFG 能否补偿？ | Day 3 下午 |
| §4.4 | DINOv2 能否提升跨形态泛化？ | Day 4 |
| §4.5 | 轮式 vs 足式平台的定量差距有多大？ | Day 5 |
| §4.6 | 哪个配置组合最适合足式部署？ | Day 6 消融 |
| §5 讨论 | 我们发现了什么？对领域有什么启示？ | 全部 |
| §6 结论 | 贡献总结 + Future Work | 全部 |

### 7.2 研究贡献总结（写在报告开头）

梳理本周工作能成为论文贡献的部分：

| 贡献 | 描述 | 证据 |
|------|------|------|
| C1 | 首次在四足机器人上验证连续 waypoint 扩散策略 | Day 5 仿真 + Day 1 基线 |
| C2 | 揭示轮式→足式迁移的定量性能差距 | Day 5 对比实验 |
| C3 | 首次量化步态抖动对扩散导航的影响 | Day 3 抖动实验 |
| C4 | 提出 DDIM+CFG 适配方案并验证其对足式的功能性价值 | Day 6 消融 |

### 7.3 代码整理

- 为 `scripts/` 下所有脚本添加完整 docstring
- 将硬编码路径统一提取为 `config.py` 常量
- 创建 `scripts/README.md` 说明运行方式

### 7.4 下阶段规划（基于本周发现）

| 本周发现 | 下一步 |
|---------|--------|
| DDIM+CFG 能满足足式控制频率 | 研究自适应 guidance scale (w 随距离衰减) |
| 步态抖动显著影响导航 | 引入 IMU 反馈做图像稳定 / 临时计算像稳 |
| DINOv2 收敛且优于 EffNet | 完整训练 100 epoch，真机对比 |
| DINOv2 不收敛 | 尝试解冻最后 2 层 / 换用 MAE 编码器 |
| 仿真对比显示足式明显劣化 | 研究 waypoint 安全约束（曲率限制） |
| 仿真验证通过 | 迁移到 Isaac Gym + Lite3 RL locomotion policy |
| 长期 | 真机 DeepRobotics Lite3 部署 |

---

### 7.5 Day 7 验收清单

- [ ] 实验报告撰写完成，**每节回答了一个科研问题**
- [ ] 研究贡献表格 (C1–C4) 描述清晰
- [ ] 所有代码注释整理完成
- [ ] `scripts/`、`results/`、`assets/` 文件夹结构清晰
- [ ] 下阶段计划文档完成
- [ ] `git commit` 所有成果

---

## 附录：项目根目录新增文件结构

> **输出命名规则**：所有脚本的可视化结果均保存到带时间戳的子目录中，
> 格式为 `results/dayN/YYYYMMDD_HHMMSS_脚本名/`，便于多次运行对比。

```text
visual-transformer/   (项目根目录, 已有的 train/、deployment/ 等不变)
├── 文档/
│   ├── 科研空白分析.md                  # Day 1: 文献空白表格
│   ├── 基于动作扩散策略...开题报告.md
│   ├── 扩散策略视觉导航调研与一周工作计划.md
│   ├── NoMaD技术详解_实现原理与Action机制.md
│   ├── NoMaD_复现指南.md
│   └── ...
├── scripts/                      # 实验脚本（本文所有 .py 文件）
│   ├── check_dataset.py            # Day 1: 数据集完整性检查
│   ├── offline_inference.py        # Day 1: NoMaD 离线推理基线
│   ├── ddim_experiment.py          # Day 2: DDIM 加速 + 控制频率分析
│   ├── realtime_inference.py       # Day 2: 实时摄像头推理 (新增)
│   ├── cfg_experiment.py           # Day 3: CFG 目标引导实验
│   ├── gait_shake_robustness.py    # Day 3: 步态抖动鲁棒性 (新增)
│   ├── nomad_vint_dinov2.py        # Day 4: DINOv2 视觉编码器适配
│   ├── train_dinov2.py             # Day 4: DINOv2 独立训练脚本 (新增)
│   ├── lite3_sim.py                # Day 5: 仿真 + 轮式vs足式对比
│   ├── ablation_experiment.py      # Day 6: 消融实验 (含足式维度, 36组)
│   └── README.md                   # 脚本运行说明
├── results/
│   ├── day1/                       # 基线可视化
│   │   └── YYYYMMDD_HHMMSS_offline_inference/
│   │       ├── baseline_trajectories.png
│   │       ├── explore_trajectories.png
│   │       └── summary.txt
│   ├── day2/                       # DDIM 对比 + 控制频率分析
│   │   └── YYYYMMDD_HHMMSS_ddim_experiment/
│   │       ├── traj_*.png          # 逐配置独立轨迹图
│   │       ├── ddim_comparison_all.png
│   │       ├── inference_hz_threshold.png
│   │       └── results.txt
│   ├── realtime/                   # 实时摄像头推理
│   │   └── YYYYMMDD_HHMMSS_realtime/
│   │       ├── recording.mp4
│   │       ├── goal_frame.jpg
│   │       ├── fps_trend.png
│   │       └── summary.txt
│   ├── day3/                       # CFG 对比 + 抖动鲁棒性
│   │   ├── YYYYMMDD_HHMMSS_cfg_experiment/
│   │   │   ├── cfg_w*.png
│   │   │   └── results.txt
│   │   └── YYYYMMDD_HHMMSS_gait_shake/
│   │       ├── shake_*.png
│   │       └── results.txt
│   ├── day4/                       # DINOv2 训练曲线
│   ├── day5/                       # 仿真 + 跨形态对比
│   └── day6/                       # 消融实验汇总 (含足式维度)
├── assets/
│   └── lite3/                      # Lite3 URDF 模型 (仿真机)
└── 实验报告.md                      # Day 7 最终报告 (回答科研问题)
```
