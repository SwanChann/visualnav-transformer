# 任务要求
完成中期汇报。

# 结果要求
生成中期报告.doc与中期汇报ppt。

# 文档参考

- 开题报告： ./开题/开题报告.doc，开题\基于动作扩散策略的足式机器人通用导航与探索研究_开题报告.md
- 项目基点：NoMad项目，中期冲刺\NoMaD_复现指南.md
- 开题到中期所做的工作内容：复现NoMad与 中期冲刺\一周详细工作流程指导.md
- 中期研究报告参考：中期冲刺\中期研究报告.md

# 对你的要求
先阅读提到的所有文档资料，搞明白要做什么内容之后再进行生成。

# 内容介绍

基于开题报告，一方面要推进项目的进展，包括复现NoMad项目，测试基线项目的推理流程；另一方面要探索视觉导航在足式机器人上面的通过性，可行性，优越性等等。需要回答为什么本来是轮式底盘导航，现在要用在足式机器人上面，在实现上有什么不一样。创新点在哪。

除了复现NoMad项目之外，需要提到训练数据集是go_standford室内导航数据集。

还做了一周工作流程里面的内容，包括：DDIM 加速采样对比，Classifier-Free Guidance 目标引导增强，DINOv2 视觉编码器升级，没做步态抖动性实验。涉及到的数据表格你要做进汇报中。有些数据没填就不做这个数据，但是所属于的表格要做进去。

还做了仿真mujoco中使用云深处的sdk_deploy，接收NoMad 的policy输出到给到机器狗速度指令的中间PD控制层。但是要注意解释数据集是真实世界的室内导航，仿真是mujoco的topomap，有些许出入，为什么还能行得通。

# 素材

- 中期冲刺\video 是在仿真中使用NoMad策略使用DDPM-10步方法或者DDIM-5步进行推理实现的导航。
- results文件夹里面有相关实验的结果和图片
- nomad训练图片里面有训练集的策略可视化和测试集的策略可视化，需要解释绿色为机器人所在位置，红色为目标位置，品红色路径为标注路径，绿色路径为探索路径，红色路径为导航路径。
- 中期冲刺\image\nomad.png是nomad算法的框架图，要在ppt的前面提到

# 下阶段研究重点
- 在仿真中策略会撞上障碍物，解释视觉编码器等等，解释为什么，下一步将开环优化成闭环
- 部署在真机实现

# 中期报告生成要求
根据中期研究报告.doc这个模版，生成完整的中期报告。

# 中期汇报ppt生成要求
根据素材和内容，在中期冲刺\ppt中用tex制作汇报文件，编译成pdf。
制作要求：
- 严格限制一页里面的内容的多少，一定不要超过页面限制，导致显示不全。
- 一页里面有多个模块，要实现放映效果。
- 要插入图片插入视频，并且做到排版
- 要有开头结尾，开头是上海交通大学，毕业设计，中期汇报，汇报人：陈样，题目等等。

# 最后要求
反复检查你生成的内容三遍，对照我给的文件和要求，若有可以改进的或者错误的地方，要修改。


===============================================
中期汇报改进：

1、对于有图片的页面进行重新排版，不要文字和图片形成左右结构，而是改成上下结构。比如说本来一个页面有两个文本框和一个图片，本来的排布是左边上下排了两个文本框，右边一个图片，现在改成上面一个文本框，下面一个图片，再下面一个文本框。这样可以让图片更大一些，更清晰一些。如果两个文本框排不下了，就只保留一个文本框，另一个文本框的内容可以放在下一页。
2、涉及到可视化的页面，尽量把图片放大一些，文字说明放在图片下面，或者图片上面，这样可以让图片更清晰一些。或者者把图片放在一页，文字说明放在下一页，这样可以让图片更大一些，更清晰一些。
3、对于有表格的页面，尽量把表格放大一些，文字说明放在表格下面，或者表格上面，这样可以让表格更清晰一些。或者者把表格放在一页，文字说明放在下一页，这样可以让表格更大一些，更清晰一些。
4、对于有视频的页面，要把视频放进来，不要只放一个链接，要把视频放进来，这样可以让视频更清晰一些。或者者把视频放在一页，文字说明放在下一页，这样可以让视频更大一些，更清晰一些。
5、下一阶段研究重点，只列举重点，不解释具体的，不写时间周期，这样可以让内容更简洁一些，更突出重点一些。
6、同时改一改中期报告.docx

================================================

ppt修改与排版要求：
1、核心科研问题 页面排版修改
2、基线轨迹可视化的导航模式和探索模式，图片的文字解释不对，根据一周工作流程中的scripts\offline_inference.py代码，重新解释图片内容，生成的到底是什么，不是只有两种颜色的路径，而是生成了8*8waypoint。
3、基线推理验证中足式导航要求15Hz，解释为什么要15Hz
4、补充DDIM原理，DDIM可以使用DDPM训练的权重，在推理时进行确定性跳步
5、仿真中发现的问题页面有错误，模型生成一整条8步waypoint轨迹后执行第一个waypoint然后重新生成轨迹，但是仍然会因为视觉编码器的误差导致机器人撞上障碍物，解释为什么会有误差。下一步将开环优化成闭环。
6、定量成果中的66Hz哪来的，检查一下，如果不对的话要改正

同时word内容要同步修改。

另外生成一份稿子。

ppt、word、稿子，生成结束后通篇检查三遍，若有问题要改正。
