# PROJECT_CONTEXT

## 1. 项目长期背景

本项目是基于 NoMaD / diffusion policy 的四足机器人视觉目标导航与部署研究项目。总体目标是在 NVIDIA Jetson Orin + 云深处 Lite3 四足机器人平台上，构建从实时第一人称 RGB 图像和目标图像输入，到视觉编码、距离预测、扩散策略生成局部 waypoint，再通过控制映射转换为 `vx, vy, wz` / `twist` 速度指令的闭环导航系统。

项目同时服务本科毕业论文写作，论文题目方向为“基于动作扩散策略的四足机器人视觉目标导航”。论文重点不是提出更大规模的通用导航基础模型，而是在真实四足机器人平台和边缘计算约束下，完成扩散式视觉目标导航策略的闭环部署、推理优化、平台适配和 sim2real 系统验证。

## 2. 核心研究定位

论文的稳定价值主张是：

本文面向真实四足机器人平台，完成视觉目标导航扩散策略从模型推理到闭环运动控制的系统化部署与实验验证。

需要避免的表述：

1. 不要把“局部航点”写成研究问题本身。局部航点是解决视觉导航问题的动作表示和控制手段。
2. 不要宣称“真机部署独一无二”。许多相关工作已有真机部署。
3. 不要硬写“首次”“最优”。
4. 不要把本文写成比 NoMaD、NavDP、SIDP 等所有工作都更强。本文价值在于明确平台、明确约束下的工程落地、部署优化、闭环频率测量、sim2real 验证和失败分析。

## 3. 论文主要贡献

1. 构建了面向四足机器人的扩散式视觉目标导航闭环系统。具体实验平台为云深处 Lite3，但贡献表述应概括为“四足机器人平台”。
2. 完成了基于目标掩码的扩散策略统一推理链路的部署优化与四足平台适配。具体模型基础是 NoMaD，但贡献表述应以方法概括为“基于目标掩码的扩散策略”，包括目标导航与无目标探索的统一推理、DDIM、CFG、TTS、航点到速度指令映射等。
3. 给出了扩散式视觉导航策略从仿真到真机的 sim2real 系统验证与分析，包括推理效率、闭环频率、任务成功率、失败模式和工程边界。

## 4. 关键实验事实

当前论文中已经使用或需要保持一致的实验事实包括：

1. DDIM-2 相对 DDPM-10 推理加速约 4.84 倍。
2. Orin 端独立高层推理频率约 7.0 Hz。
3. 真机闭环频率：DDPM 基线约 2.89 Hz；最快配置 DDIM-2 + CFG=0 + TTS-8 约 6.80 Hz；增强配置 DDIM-2 + CFG=2 + TTS-8 约 3.43 Hz；对比配置 DDIM-3 + CFG=0 + 无 TTS 约 5.90 Hz。
4. 真机多场景目标导航实验中，四组配置均达到 100% 成功率。
5. 探索模式在真机上可用，室外零样本实验暴露了光照、纹理、开阔场景泛化等边界。

## 5. TTS 术语约定

论文中的 TTS 不是 text-to-speech，而是 test-time scaling / inference-time scaling，也可以称为推理时搜索或推理时扩展。

TTS 不是一个固定算法，而是一类在推理阶段增加候选、搜索、重排序或验证预算以提升输出质量的思想。本文采用的是其中的 best-of-N 形式：

1. 在推理阶段采样 N 条候选航点轨迹。
2. 使用启发式评价函数或 verifier 对候选轨迹打分。
3. 选择最优轨迹或前 K 条候选进入执行。

论文中应强调层级关系：

1. DDIM 决定每条候选轨迹如何生成，主要用于减少扩散采样步数和降低时延。
2. CFG 决定目标条件牵引强度，用于调节目标一致性。
3. TTS / best-of-N 决定多条候选轨迹生成后如何选择，不是新的训练方法，也不是新的扩散采样器。

## 6. 相关工作判断边界

相关工作写作时应采用“区分研究侧重点”，而不是贬低已有工作。

1. NoMaD 是本文复现、优化和部署的直接基础。NoMaD 统一了目标导航和无目标探索，但公开实验主线更偏轮式移动平台和通用导航评估，对足式平台视觉扰动、速度接口、安全状态和端侧算力约束讨论较少。
2. NaviBridger 是 RGB-only + goal image 的扩散桥视觉导航工作，有真实室内外实验。差异在于其重点是有目标条件下的扩散桥动作生成，对目标导航与无目标探索统一系统、四足平台闭环部署效率讨论较少。
3. NavDP 强调大规模仿真数据、特权信息指导、跨机器人形态迁移和 zero-shot sim2real，并在轮式、四足和人形平台上展示较强泛化。不能否认其 sim2real 能力。本文差异在于具体四足平台上的单目图像目标 / 拓扑目标驱动闭环系统、速度接口适配、闭环频率评估和工程分析。
4. SIDP 报告了较快的纯推理时间，尤其是 Jetson Orin Nano 上的模型效率。本文不能直接说纯推理更强，差异在于本文给出真实机器人完整闭环频率和系统执行链路。
5. DiPPeST 面向四足机器人做扩散路径规划和视觉伺服，有真实四足执行验证。差异在于本文关注目标图像 / 拓扑节点驱动的视觉目标导航闭环、边缘部署效率和 sim2real 系统分析。
6. QUAR-VLA 关注四足机器人的视觉-语言-动作任务，不是本文这种弱先验室内单目图像目标导航部署问题。

## 7. 关键目录和文件

- `论文/main.tex`：论文主入口。
- `论文/contents/intro.tex`：绪论，包含国内外研究现状和本文主要贡献。
- `论文/contents/algorithm_design_and_experiments.tex`：第二章算法设计与实验，包含 2.3 动作扩散策略、NoMaD、DDIM、CFG、TTS 等内容。
- `论文/contents/framework_and_simulation.tex`：仿真系统与 MuJoCo 相关章节。
- `论文/contents/real_robot_experiments.tex`：真机部署与实验章节。
- `论文/contents/conclusion.tex`：总结与展望。
- `论文/refs.bib`：参考文献库。
- `毕设冲刺/国内外研究现状修改.md`：国内外研究现状的中间稿和修改依据。
- `毕设冲刺/原有算法框架优化与实验教程.md`：NoMaD、DDIM、CFG、TTS、视觉编码器实验思路。
- `毕设冲刺/原有算法框架优化与最终方案总结.md`：算法优化方案和证据边界总结。
- `毕设冲刺/Orin上Lite3真机部署完整教程.md`：Orin + Lite3 真机部署链路说明。

## 8. 常用命令

在论文目录下编译：

```powershell
cd f:\codespace\visualnav-transformer\论文
latexmk -xelatex -interaction=nonstopmode -halt-on-error main.tex
```

检查引用和关键术语：

```powershell
rg -n "\\cite\\{|TTS|DDIM|CFG|NoMaD|NavDP|SIDP|NaviBridger" 论文
```

Windows PowerShell 读取中文 UTF-8 文件时建议设置输出编码：

```powershell
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
Get-Content -Raw -Encoding UTF8 论文\contents\intro.tex
```

